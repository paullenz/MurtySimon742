# Recovery and continuation handover

11 September 2026. Active coordination continued here at Paul's request after another chat timed out. The repository remains the durable project record.

## Recovered baseline and limits of retrieval

The repository API confirmed owner/name `paullenz/MurtySimon742`, stable repository id `1359206057`, default branch `main`, and baseline commit `b104846d222f09cb0d8bbf25ff9f51f6d1165bdf`. Do not substitute the historical name `MurtySimon25` in new links or writes.

Prior-chat search returned incomplete and partly stale handover material. It did not recover a verbatim timeout transcript or establish any uncommitted work. In particular, a retrieved earlier note still called the A1 `D1=2` branch open; the current repository contains its completed hand elimination. Current saved files and commit history therefore determine this recovery. No claim is made that inaccessible uncommitted calculations have been recovered.

The last baseline commit hardened rational Farkas reconstruction in the simplified N30 two-transport program. Its GitHub Actions run `34637053244` completed successfully. The workflow uses the separate exact wrapper to require certificates; the changed exploratory script's numerical status alone is not accepted as proof.

Read and applied: `project/N25_PROJECT_STANDING_ORDERS.md` and `project/REPO_SYNC_POLICY.md`, including evidence hierarchy, preservation, current reviewer links, non-forced publication and read-back verification. There is no `AGENTS.md` in the recovered repository tree. No sub-agents were used.

## Reconciled project map

These are the saved project claims, not a new full audit of every historical proof:

- N25, N27, N28, N29 and N30 have complete candidate fixed-order results; independent specialist review remains open. The current reviewer editions are v2 at N25/N27/N28/N30 and v4 at N29.
- N29 reviewer-v4 has a hand route for the full candidate statement, with no proof-critical finite search. Its frozen bridge needs the additive source-degree display erratum recorded in this continuation.
- The general maximum-degree `7/12` result remains the strongest reviewer-packaged candidate. No unrestricted conjecture proof or complete order above 30 is claimed.
- In the separate general-N regime programme, the `t=3,(h_res,J)=(4,14)` A1 regime now has a complete candidate hand reduction. The next adjacent symbolic targets are A530 `(4,16)` and `(4,18)`; the old open `D1=2` handover is superseded.
- At N30/Delta16 the later threshold-tail notes give a candidate hand route excluding `m>=226`. The frozen reviewer-v2 retains its original finite route.
- At N30/Delta16/m225 the saved 100 demand profiles reconstruct 272 tail-slack rows. The earlier ledger argument removes 61; four zero-demand tight rows have exact Hall certificates; 207 positive tight rows were the active simplification target.

The top-level README and reviewer starting page had not caught up with both the late N30 hand reductions and the complete A1 regime. They are synchronised in this publication. No existing reviewer release is renamed or promoted.

## Work completed here

1. Replayed the old N30 hand-profile classification, eight-row hand endpoint table, 272-row tail-slack reconstruction and four zero-demand exact certificates. All pass; full outputs are preserved under `recovery/`.
2. Derived the scalar source-excess/Hall criterion in `SCALAR_EXCESS_HALL_LEMMA.md`. It uses tightness, the correct supplement bound and `q<=#{s<=rho}`; the supplement variable attains its minimum at just three explicit endpoints.
3. Used floating LP discovery, followed by integer-weight compression, to obtain seven explicit templates. All reported acceptance is subsequently recomputed in exact arithmetic.
4. Wrote a separate standard-library checker that reconstructs the frontier without importing historical/discovery programs. It tests all templates on all 211 tight rows and evaluates source minima both directly and by the derived endpoint formula.
5. Verified 57 disjoint positive-row exclusions; the exact minimum assigned gap is `1/12`. Saved all 57 rows, the complete 150-row positive remainder, discovery diagnostics, small-denominator attempts and exact evidence.
6. Found the omitted `q_u` in the frozen source-degree display. Preserved the original and added an erratum proving the correct identity and checking the relevant inspected source domains. This is non-blocking and does not change the stated supplement bound.

## Validation boundary

The new result is **candidate analytic reduction + REPRODUCED arithmetic**, not a PROJECT-CERTIFIED ledger promotion or external endorsement. The checker does not prove completeness of the 100 demand profiles or the universal graph bridge. All 150 remaining rows are already addressed by stronger preserved methods in the existing candidate programme; they survive only the new weaker scalar test.

The saved LP's nonpositive results on the other 154 tight rows do not prove an impossibility theorem about scalar certificates. The seven-template cover is not asserted minimal. No `t+1` extrapolation is made.

## Next justified mathematical step

Begin with `REMAINING_150_POSITIVE_ROWS.json`. Seek a small shared inequality incorporating the residual-column budget and joint label/source constraints omitted by the scalar test. The seven previously hard N30 profiles have demand minimum one and remain in this list; their existing two-template 3-D certificates remain available.

An independent secondary target is the hand classification of the 100 `Q>=18` demand profiles. The new inequalities do not discharge that obligation. The general A530 cells are a separate valid continuation path; do not confuse their t3 regime counts with this seven-template N30 scalar cover.

Before a new current reviewer edition is built, audit these supplements against the complete frozen proof, preserve every original source, update every reviewer entry point and check the exact committed paths. The present publication is a research checkpoint and additive erratum, not a newly built PDF release.

## Reproduction

Acceptance:

```sh
python -I -B project/research/n30/2026-09-11-m225-hall-continuation-v1/verify_scalar_certificates.py
```

Optional discovery (Python 3.12.14 and SciPy 1.17.0 in this run):

```sh
python -B project/research/n30/2026-09-11-m225-hall-continuation-v1/mine_scalar_excess.py
python -B project/research/n30/2026-09-11-m225-hall-continuation-v1/compress_scalar_excess.py
```

Discovery may return a different valid small-weight cover under a different solver implementation; the preserved integer certificate is the exact replay input. Discovery output never substitutes for the standalone acceptance check.
