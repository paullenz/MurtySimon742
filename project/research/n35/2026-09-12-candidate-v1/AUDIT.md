# N35 focused hostile internal audit

12 September 2026. No blocking flaw found in the new finite reduction,
certificate replay and proof assembly. This is same-assistant internal review;
external mathematical review, novelty and external reproduction remain OPEN.

## Degree and edge coverage

At m>=306 average degree forces Delta>=18. The balanced-degree dependency
handles Delta=18 including equality. The a=14,b=20 theorem gives m<=300 at
Delta=20. The 7/12 theorem applies to every Delta>=21. At Delta=19 the
fifteen-label score bounds t by three; both t=3 and t=2 are fully covered.
The explicit K(17,18) converse checks equality is attained by a D2C graph.

## Frontier completeness and reuse

The original CSV retention threshold is monotone Q>=20 with s_i<=13. The
N35 thresholds are 25 and 23, so no relevant profile was filtered out. Its
demand-14 filter is justified because N35's isolated-C argument gives d<=13.
The new expander computes direct tail sums and recursively enumerates sorted
residual multisets above the minimum partition; it does not call the old
breadth expansion. `audit_frontier.py` compares both complete state sets,
not only counts. Agreement: 13 profiles/19 states and 144 profiles/466 states.
The shared complete demand CSV remains a stated common dependency.

## Budget, zeros and local domains

Positive demands force S=r+2t exactly. The first hand filter is safe only
under min(s)>0; it never excludes a zero-demand state by that rule. All three
zero-demand states have one zero and E=0. The independent formula verifier
uses d=R-E on a zero label and retains every feasible R. No assertion that
R=0 is made. Source supplement caps are rho+3, reflecting b=19, and load
capacity is 19. All source and label options are checked in integer arithmetic.

## Threshold and potential implications

The tight-subset rule requires exact capacity equality, h>=2 and z>=h. The
source-capped rule retains every individual source capacity and all possible
counts of high selected outdegree sources. Its pair limit is recomputed as
`j(z-j)+j(j-1)/2`, independently of the discovery expression.

The potential basis is increasing in s,d and decreasing in endpoint load.
The unchanged fixed load cutoffs from N34 remain valid indicators at N35;
they need not shift when b changes. Free lambda,c,mu may have either sign,
while all basis weights and tail multipliers must be nonnegative. Exact
formula replay does not import discovery, SciPy or NumPy. Every integer
coefficient name, domain and strict gap is checked. Complete state IDs,
state tuples and duplicate detection prevent a successful subset from being
mistaken for full coverage.

The new N34 p<=4 hand theorem is not applied to this order. That would be an
invalid automatic transfer to generic N35 sources, whose rho=2 bound is p<=5.

## Results and remaining trust boundary

All 485 states have disjoint exact exclusions. The total is 228 hand/accounting
arguments plus 257 envelopes and 92,701 local integer checks. All three zero
states are covered. No full RX/Hall or heavy Farkas model is required. Numerical
proposal failures remain in the streams and are not assigned proof status.

The N35 candidate still depends on prior candidate structural arguments and
the canonical graph-to-incidence implications. This audit does not establish
their novelty, external acceptance or formal verification. The enumeration
and exact certificates remain proof-critical finite evidence.

## Documentation correction

The current README and reviewer entry point still contained a stale governance
sentence saying no complete candidate above n=33, despite their N34 headline.
This pass corrects that limit to n=35, synchronizes N34 reviewer-v2 and N35
reviewer-v1 throughout current navigation, and explicitly preserves historical
package snapshot hashes. The stale sentence was a documentation inconsistency;
no certificate or mathematical conclusion was retracted.
