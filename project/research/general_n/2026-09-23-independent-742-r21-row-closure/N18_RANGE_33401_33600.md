# n=18, Delta=10 exact stable-index range 33,401–33,600

Status: internally verified computational evidence only. This is an abstract assigned-witness feasibility screen, not graph realizability and not external mathematical acceptance.

The current exact stable-index enumerator and aggregated witness-deficit MILP were replayed on stable indices 33,401 through 33,600. All 200 requested candidates were enumerated and the range completed. There were zero new abstract survivors. The closest candidate was index 33,401 with demands `(4,4,3,1,1,1,1)`, x=`(5,5,4,2,2,2,1)`, h=`(6,6,5,3,3,3,1)`, exact minimum deficit 25 against Dmax 16, hence gap +9. Every solved row reported HiGHS optimal status 0.

The emitted JSON was independently parsed after execution: `range_complete=true`, `enumerated=200`, `stable_last_seen=33600`, empty survivor list, and the closest-row values above. The shell wrapper timed out only after the complete JSON had already been emitted; in the helper the print occurs after the multiprocessing pool context closes, so the saved complete result is the evidence used here.

Consequently the internally screened prefix extends through stable index 33,600, apart from the previously isolated index 20,851 which is separately excluded only by the repository's audited graph-level 555 model. Full n=18, Delta=10 row closure is still open. Next exact stable index: 33,601.

Trust limits remain unchanged: internal evidence only; abstract feasibility is not graph realizability; no new theorem claim; equality controls and X_3 negative control are untouched; the external e+disj+X proof was not used or reconstructed.
