# Complete strict r=10 residual closure

Status: **VERIFIED INTERNAL COMPUTER-ASSISTED RESULT; NOT EXTERNAL VERIFICATION.**

The final support-nine partition `R=(2,1,1,1,1,1,1,1,1)` has 2,208,612 rooted colour-preserving core orbits. The optimistic core ledger leaves 146 positive-surplus candidates. Exact physical-source feasibility eliminates all 146.

Combined with the already saved support-at-most-eight closures, every partition of residual demand `r=10` is internally closed. Thus strict surplus satisfies `t<=0` at r=10, extending the internal edge bound through total demand `S<=12`.

Trust limits: the result is finite exact computation within the independently developed profile pipeline. It is not external verification, does not prove the general theorem, and does not extend the balanced complete-bipartite equality characterization beyond the previously checked `S<=8` scope.
