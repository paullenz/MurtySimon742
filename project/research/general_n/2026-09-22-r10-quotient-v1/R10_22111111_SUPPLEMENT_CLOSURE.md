# r=10 partition (2,2,1^6): supplement closure

Status: **VERIFIED INTERNAL COMPUTATION; NOT A GRAPH-REALIZATION THEOREM.**

The exact physical-source screen left masks `15402149` and `48374421`. Exact multiset enumeration gives exactly one source population for each. The supplement condition eliminates both populations: zero supplement-feasible cases in each kernel.

Thus both r=10 support-eight partitions are internally closed. The sole support-nine partition `(2,1^8)` remains, so no r=10 or S-bound extension is claimed here.
