# r=10 partition (2,2,1^6): exact quotient/source census

Status: **VERIFIED INTERNAL COMPUTATION; NOT A GRAPH-REALIZATION THEOREM.**

For residual demand vector `R=(2,2,1,1,1,1,1,1)`, exact enumeration modulo the colour-preserving group `S2 x S6` gives:

- 156 unlabelled unit-vertex graphs;
- 250,240 rooted/coloured core orbits;
- 792 orbits with positive optimistic strict upper bound;
- exactly two orbits satisfying the exact physical-source necessary condition, masks `15402149` and `48374421`, each with upper bound `t=1`.

The saved JSON includes exact source witnesses. This is only a necessary-condition screen: both cores still require supplement forcing, and neither is asserted realizable by a diameter-2-critical graph.
