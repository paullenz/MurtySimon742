# Exact supplement closure of the `r=8` equality boundary

Trust level: internally checked, computer-assisted necessary-condition exclusion. This is not external verification of the full conjecture.

For the 39 source-feasible equality-core orbits, exact multiset enumeration produced **2,103 physical-source populations**. Necessary B-layer supplement forcing eliminates every population: **zero supplement-feasible survivors**.

The forcing test requires every selected incidence `(u,i)` to be isolatable by one prospective B-edge. It tests edges to sources in the population and, optimistically, to every valid residual-free N-state source type. A usable edge must force exactly the singleton incidence; A-common-neighbour and criticality constraints are relaxed, so failure is a valid necessary obstruction.

Consequently the internal finite pipeline now gives

`1<=r<=8  =>  f<=r-1`.

Under the audited equality translation, this extends the internally checked balanced complete-bipartite equality characterization from total residual demand `S<=7` to **`S<=8`**. Balanced complete bipartite graphs remain the mandatory equality control and are not represented by an excluded positive-residual defect profile.

Combined with the strict `r=8` closure, the current internally checked thresholds are: edge bound through `S<=10`, equality characterization through `S<=8`. The positive-demand strip and the general theorem remain open.
