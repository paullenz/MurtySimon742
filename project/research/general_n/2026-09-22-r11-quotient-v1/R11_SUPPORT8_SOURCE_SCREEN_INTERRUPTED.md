# r=11 support-eight source screen: interrupted monolithic run

The exact source-screen program was launched against all 2,496 strict support-eight cores at 16:35:17 BST. It had not produced its final aggregate by 16:52:33 and was interrupted before the research cutoff to preserve the session.

No feasibility or infeasibility conclusion is drawn from the interrupted run. The durable program and full 2,496-row input ledger are saved. The next run should shard the screen by the three residual partitions `(4,1^7)`, `(3,2,1^6)`, `(2,2,2,1^5)`, publishing each completed aggregate separately.

Process note: the in-progress checkpoint was written at 16:45:36, 19 seconds beyond the ten-minute preservation backstop. This is recorded as a compliance defect rather than hidden.
