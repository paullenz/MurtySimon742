# Preserved exploratory work

The state3349 whole-state enumeration was interrupted after eight seconds (exit124), with no output and no conclusion. Compile scan_saturated_prefix.cpp and use PILOT_INPUT.txt for that single-state pilot; SATURATED_TARGET_INPUT.txt contains all24 affected states. No completed enumeration is claimed.

The forced-label flow file is preserved as originally explored, without independent min-cut verification. For replay, copy the script and ../../2026-09-15-saturated-receiver-barrier-v1/RESCUE_PROFILES.json into a temporary directory, run the copied script there, and compare its FORCED_LABEL_FLOW_RESULTS.json with the frozen output here. Its claims are fixed-q only. The subsequent scalar tight-block/equality proofs have separate direct arguments and independent checks.
