# Exact supplement closure of the two support-seven r=9 kernels

22 September 2026. Internally checked computer-assisted necessary-condition exclusion. This extends only the bounded r=9 finite pipeline through residual support seven; it is not a full r=9 theorem because support eight remains.

The prior quotient/source screen left exactly two physical-source-feasible cores in residual partition (2,2,1,1,1,1,1), canonical masks 120436 and 378090. The saved checker exhausts unordered physical-source populations with exact residual column sums and selected lower bounds, then applies the same exact B-edge supplement-forcing kernel used in the audited r=8 closure.

Results:

- mask 120436: 276 allowed single-source patterns, exactly 1 feasible source multiset, 0 supplement-feasible populations;
- mask 378090: 286 allowed single-source patterns, exactly 2 feasible source multisets, 0 supplement-feasible populations.

Thus every r=9 optimistic strict-surplus core with residual support at most seven is eliminated by the existing core/source/supplement necessary conditions. The all-unit support-nine case is already excluded by the unit-column theorem. The only unclosed residual partition for strict r=9 is therefore (2,1,1,1,1,1,1,1), support eight. A symmetry-aware enumeration or analytic reduction of that partition is the next step.

Trust boundary: source/supplement infeasibility is a necessary-condition contradiction and therefore closes the enumerated finite cases internally; no surviving source population would by itself have been an actual graph.
