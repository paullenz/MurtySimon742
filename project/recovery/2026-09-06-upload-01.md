# N=25 recovery intake 01 — 6 September 2026

## Result

**PARTIAL_SOURCE_RECOVERY. No theorem or certification status has changed.**

The user supplied 19 individual files: the full-checkpoint archive manifest and all 18 root-level files named in it. Every supplied payload matches both the byte count and SHA-256 in the supplied manifest. The manifest itself has SHA-256 `91eb0390cf8d79022a0dbd9dafdf53182fdf852e3ad9fba2e5908234b04e88ed`.

This establishes consistency with the user-supplied manifest, not independent authentication of the original ZIP. The original ZIP has not been supplied, so its previously recorded SHA-256 has not been checked.

| Intake measure | Result |
|---|---:|
| Individual files received, including manifest | 19 |
| Received bytes | 255,102 |
| Payload entries declared in the manifest | 773 |
| Declared payloads received and hash-matched | 18 |
| Declared payloads received but mismatched | 0 |
| Declared payloads not supplied in this batch | 755 |
| Bytes declared for missing payloads | 386,008,773 |
| Uploaded Python scripts passing syntax compilation | 13 |
| Historical proof traces replayed during this intake | 0 |

Syntax compilation did not execute the uploaded scripts, import their dependencies, run a solver, or test mathematical correctness. Original bytes were copied unchanged into a separate intake bundle.

## Recovered scope

The checkpoint concerns Delta=14, k=5, r=12..17 and k=6, r=3..12. Its top-level files include the SAT encoder, experimental MILP filter, partition and full-survivor drivers, proof generation/replay/repair drivers, catalogue and ledger audits, formula-hash regeneration, packaging, dependency pins, status and runbook. The `k5` filenames do not mean that all these scripts are limited to k=5; the supplied runbook explicitly documents k=6 support for the main SAT and batch tools, with the MILP tool remaining k=5-only.

No Delta=15/v4 material or latest connected-k=2 certificate corpus was supplied in this batch. The global mathematical frontier must not be overwritten with this earlier checkpoint's historical scope.

## Payloads still needed from this checkpoint

| Directory | Missing entries | Declared bytes |
|---|---:|---:|
| `catalogues/` | 16 | 620,230 |
| `certificates/` | 667 | 312,168,160 |
| `recovered/` | 26 | 2,693,492 |
| `results/` | 46 | 70,526,891 |
| Total | 755 | 386,008,773 |

These are sizes of the listed files, not a prediction of the enclosing ZIP's size.

The certificate directory entries include 304 k=5 and 345 k=6 active full-stage RUP files, four current/pre-repair manifests, and 14 quarantined historical files. The zero-byte quarantined entries are explicitly historical; their listing is not evidence that this upload is corrupt. None of those subdirectories was supplied here. The results directory also lists a separate test proof copy.

`includes_full_proof_files: true` describes the source archive represented by the manifest. It does not mean the individual files uploaded here contain those proof bytes.

## Historical results versus checks performed now

The supplied `CHECKPOINT_STATUS.json` records 62,023 catalogue records, 61,922 target-branch records, 4,542 regenerated CNF hashes, 304 k=5 full-stage replays and 345 k=6 full-stage replays. It explicitly sets `whole_branch_certificate_complete` and `journal_grade_claim` to false. These are recovered historical statements, not newly reproduced results.

The progress note says the full-stage proofs cover only survivors of the weaker screen. Screening UNSAT exclusions still need certificates or audited hand arguments. It also distinguishes project checker replay from independent review of mathematical necessity and catalogue completeness. This intake does not alter those qualifications.

A new, separate intake checker validates manifest paths, sizes and hashes without executing project code. It reports 18 matches, zero mismatches and 755 missing entries, with exit code 2 for partial recovery. Nine regression checks passed for complete, missing, altered, duplicate, unsafe-path and symlink inputs. These are tests of the intake byte checker, not of a SAT-proof checker.

## Preservation and repository status

The complete received batch is preserved unchanged in the downloadable `N25_Recovery_Upload_01_2026-09-06.zip`, under `originals/`, alongside a detailed receipt, all 755 missing paths and hashes, an intake checker and its test results. This is a newly created partial-recovery bundle, not the original checkpoint release.

This repository record and its companion `2026-09-06-upload-01.sha256` document intake. They do **not** contain or replace the 19 original payload files. Those originals are staged in the conversation bundle; copying their payloads into GitHub remains pending. Do not mark the full checkpoint, source/results ZIP or any of the 23 hash-pinned original release artifacts as recovered on the strength of this receipt alone.

## Next recovery action

Obtain the unchanged `delta14_full_checkpoint_2026-09-06.zip`, expected SHA-256 `3c53614d1a229bf6cf411703547919c874c544ac2c0f1e911a867c51d7de9e2e`, or the complete four missing subdirectories. The original ZIP is preferable because its container hash and internal paths can both be checked.

Delta=15/v4 and the later connected-k=2 completion corpus remain separate high-priority recovery tasks. No mathematical ledger promotion is justified by this intake.
