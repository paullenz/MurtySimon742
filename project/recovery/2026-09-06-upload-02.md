# N=25 recovery intake 02 — 6 September 2026

## Result

**COMPLETE_MANIFEST_PAYLOAD_RECOVERY. No theorem or certification status is promoted.**

The received `delta14_checkpoint-2.zip` contains all 773 payloads in the previously received full-checkpoint manifest, plus that same manifest. All payload sizes and SHA-256 hashes match; none is missing or mismatched. This closes the 755-file content-recovery gap from intake 01 for this checkpoint.

| Check performed in this intake | Result |
|---|---:|
| Declared payload files matched | 773 / 773 |
| Payload files missing / mismatched | 0 / 0 |
| Previously missing payloads recovered | 755 |
| ZIP CRC checks, including metadata | Passed |
| Active k=5 proof files: gzip and both hashes | 304 / 304 |
| Active k=6 proof files: gzip and both hashes | 345 / 345 |
| Freshly regenerated graph catalogues | 16 / 16 byte-identical |
| Catalogue records in those files | 62,023 |
| Arithmetic rejection records recomputed | 233,187 |
| Full-stage case / proof / historical replay links | 649 / 649 matched |
| C++ checker regression cases | 4 / 4 passed |
| Intake-checker regression tests | 9 / 9 passed |
| Project Python files successfully syntax-parsed | 23 |
| Fresh historical proof-clause replays | 0 |
| Fresh CNF-hash reconstructions | 0 |

## Container identity versus payload identity

Uploaded archive: 319,171,345 bytes; SHA-256:

`27d4a632dc8bc2262def45b0a3ea0650adb8fb5d7e831e19839d14a3f8af9dfe`

The prior repository pin for `delta14_full_checkpoint_2026-09-06.zip` is:

`3c53614d1a229bf6cf411703547919c874c544ac2c0f1e911a867c51d7de9e2e`

These container hashes differ. The received archive uses the wrapper `delta14_checkpoint-2/` and includes 802 `__MACOSX` metadata files. It is preserved under its received identity, not renamed or certified as the original release ZIP. Nevertheless, its manifest is byte-identical to intake 01 (SHA-256 `91eb0390cf8d79022a0dbd9dafdf53182fdf852e3ad9fba2e5908234b04e88ed`), and all 773 declared payloads match that manifest. No unexpected project files, duplicate ZIP names, unsafe paths, encrypted members or symbolic links were found. The original ZIP is no longer needed to recover this checkpoint's contents, although its exact historical container remains an archival obligation.

The four previously missing directories are fully recovered: `catalogues/` (16 files), `certificates/` (667), `recovered/` (26), and `results/` (46). The 14 zero-byte historical quarantined proof copies match their manifest entries. They are preserved as history and are not used as active certificates.

## Additional original artifact recovered

`recovered/delta14_k7_audit_tools_2026-09-05.zip` is present, its internal ZIP CRC check passes, and its 74,523 bytes have SHA-256 `62812bc3edc640befd095f89b63c983960f36204fae3b648308401e9c66e2f25`. This exactly matches the pre-existing repository recovery pin. One of the 23 originally hash-pinned missing artifacts is therefore recovered as an exact original container, separately from the repackaged full checkpoint. Its five k=7 certificate archives are not included here.

## What the fresh checks establish

The C++ project checker was rebuilt from the received source and passed its four supplied positive/negative examples. These tests are not formal verification of the checker, and they are not replay of the historical 649 proofs.

All 16 catalogues were regenerated using a fresh `geng` executable built from the received, hash-pinned nauty/pynauty source tarball. Every catalogue is byte-identical to the corresponding recovered file. This is new reproduction using the same nauty implementation, not an independent graph-generation algorithm.

The coverage and arithmetic audit reran the archived audit function bodies using an explicit dependency adapter. It loaded only the encoder's pure graph-decoding and component functions; CNF building was disabled rather than imitated. The adapter pins both source-file hashes and refuses Python's optimized mode. It accounts for all catalogue indices, residual partitions, 233,187 arithmetic rejections and 57,569 screening calls. A separate fresh reconciliation matches the 304 k=5 and 345 k=6 full-stage cases to their proof manifests and historical replay records.

The first version of the new reconciliation check assumed every proof record had an explicit `max_degree_f`. The older k=5 records omit that field, and the archived replay driver explicitly defaults it to four. The adapter was corrected to use that same convention and the final full run passes. No original input was repaired or altered.

## What has not been checked anew

PySAT is not installed in this runtime. Installing the pinned dependencies failed because the package host could not be resolved. That failure does not establish that the requested package version is unavailable elsewhere. The runtime is Python 3.13.5 rather than the historically recorded 3.12.13.

Accordingly, no new reconstruction of the 4,542 historical CNF hashes and no new clause-by-clause replay of the 649 historical traces is claimed. Gzip/hash checking and matching old replay records are not substitutes for those steps.

The whole-branch screening-certificate gap remains. Of 57,569 recorded screening calls, 649 passed to the full stage; this leaves 56,920 recorded screening UNSAT calls not certified by this intake. The source checkpoint also explicitly leaves independent mathematical necessity, shared-lemma and catalogue-completeness review outstanding. No new elimination, journal-grade claim or resolution of N=25 follows from file recovery.

This batch does not recover the Delta=15/Audit-v4 dossier, the reproducibility-v1 original release, or the later connected-k=2 completion corpus. Global branch statuses must not be replaced with this earlier checkpoint's historical frontier.

## Preservation and GitHub publication

The received ZIP and all 774 project files (773 payloads plus manifest) are preserved unchanged in the conversation workspace. The separately generated `N25_Recovery_02_Source_Results_and_Audit_2026-09-06.zip` contains the recovered source, catalogues, result records, vendor archive, manifests and new checks. Its README explicitly identifies the 663 active/quarantined proof-directory `.rup.gz` files omitted from that compact companion; those bytes remain in the received full ZIP.

Eight exact original source/status files were committed to `main` at `add33739ba927c27b4b1c947b028b13ac77a7be3`, under `repro-v1/recovered/delta14-checkpoint-2026-09-06/`: the SAT encoder, replay driver, proof-integrity auditor, requirements, checkpoint status, progress note, C++ checker and checker regression suite. Their Git blob hashes and byte sizes were read back and matched against the received originals.

The full 319 MB received archive, the nested k=7 tools ZIP, the other original payloads and the large detailed check outputs are **not yet published on GitHub**. A receipt or a hash is not a substitute for those bytes. This report, its compact machine-readable results and the recovery helper code document the current recovery without claiming a complete external-audit release. The theorem ledger is unchanged.
