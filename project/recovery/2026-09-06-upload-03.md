# N=25 recovery intake 03 — 6 September 2026

## Result

**VERIFIED_COMPACT_SUBSET_OF_PREVIOUS_CHECKPOINT. No new mathematical evidence or theorem-status change.**

Received `delta14_checkpoint.zip` (7,233,301 bytes). SHA-256:

`df83e152ccd9437ad85dc32a6bb3dab7e4aaafb242ecd3322344cff38eadba13`

All 110 declared payloads were read from the ZIP, checked against their internal manifest by size and SHA-256, and directly compared byte-for-byte with the corresponding entries in the previously received `delta14_checkpoint-2.zip`. All 110 match both checks. The manifest itself is an additional project file, so there are 111 project files in this smaller archive.

| Check | Result |
|---|---:|
| Payloads matching the supplied manifest | 110 / 110 |
| Payloads byte-identical to the larger checkpoint | 110 / 110 |
| New or changed payloads | 0 |
| ZIP CRC integrity | Passed |
| Unexpected or missing project members | 0 |
| Unsafe paths, duplicate ZIP names, encrypted or symlink entries | 0 |
| macOS metadata files | 123 |

## What differs

The compact manifest has `includes_full_proof_files: false` and SHA-256 `770e67fc11c97ff6e67b0a4ae23b9858afcb19caafa6f9550ab2c3153b31cc30`. It lists 110 payloads rather than the larger checkpoint's 773. It is a different administrative inventory, not new mathematical evidence.

The compact package contains 18 root-level payloads, 16 catalogues, four certificate manifests, 26 recovered-source/provenance files and 46 results files. The only payloads present in the larger checkpoint but absent here are 663 proof-directory `.rup.gz` files: 649 active proofs and 14 quarantined historical copies. A separate test proof copy remains in `results/`; this is not a substitute for the bulk proof corpus.

The current container hash does not match the original compact-release pin `503a5cfbae2a4c25bd074086a4f30cbe19915be967475ec40fc10f6a3dc4ac39`. Preserve it under its received identity, not as a hash-authenticated original release. It is a repackaged compact source/results copy whose 110 payloads are verified duplicates of the already recovered full checkpoint.

Comparison reference: `delta14_checkpoint-2.zip`, 319,171,345 bytes, SHA-256 `27d4a632dc8bc2262def45b0a3ea0650adb8fb5d7e831e19839d14a3f8af9dfe`. The full recovery is documented in `project/recovery/2026-09-06-upload-02.md`.

## Consequences

This upload is useful as a smaller source/results working copy and confirms unchanged content across these two received containers. It adds no Delta=15/Audit-v4 material, no reproducibility-v1 release, and no later connected-k=2 completion corpus. It is not independent mathematical corroboration. Retain the larger checkpoint as the complete recovered evidence set.

This intake did not execute uploaded project code, regenerate SAT formulas, or replay proof clauses. No theorem status is promoted, and no already-recovered proof or catalogue is counted a second time.

## Preservation

The received ZIP remains unchanged in the conversation workspace. The comparison helper and detailed JSON results are preserved in `N25_Recovery_03_Comparison_2026-09-06.zip`. This receipt records archive identity and content comparison; it does not claim publication of the ZIP or bulk evidence to GitHub. The earlier publication obligations remain open.
