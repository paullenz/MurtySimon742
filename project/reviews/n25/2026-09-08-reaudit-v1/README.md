# N=25 re-audit — in-progress execution checkpoint

8 September 2026. This audits the frozen reviewer edition, without invoking the later general-degree results. Mathematical status remains CANDIDATE; independent specialist review OPEN. Frozen sources, original evidence and the theorem ledger are not changed.

The local repeat of the earlier third arithmetic audit completed all 543,578 outer states, 3,442,212 labelled columns and 1,959 equality certificates. A new terminal checker independently reconstructs the final caps using labelled-subset matching. The new mutation tests expose the original modular checker's declared reliance on prior ledger caps and its ability to report unresolved survivors; the stricter new endpoint requires independently derived caps and zero survivors. The known matching-helper defect outside the production dimensions was reproduced.

The initial hosted monolithic replay did NOT finish. Run 34212897538, job 102017807363, received a runner shutdown signal with exit code 143. The original Delta14/157 stage had completed with matching outputs and tests. No mathematical failure or specific resource cause is inferred from that interruption. The old workflow is preserved in history.

`band_replay.py` now reruns both original generators and classifiers unchanged in sixteen disjoint groups, checking full state/column data immediately instead of storing whole-scope pretty JSON. A group PASS is not a full replay; every group and aggregate coverage must pass. The new workflow separately reruns the previous third audit, the new terminal checker and mutations. The final report and execution receipts will distinguish completed work from the interrupted original wrapper. This checkpoint does not claim the new hosted runs have completed.
