name: Forced core receiver capacity

on:
  push:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Replay forced-core receiver-capacity proof
        run: python3 project/research/general_n/2026-09-15-forced-core-capacity-v1/run_replay.py
