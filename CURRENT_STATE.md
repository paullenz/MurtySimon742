# Dense diameter-2-critical research - live current state

> **Active target - 19 September 2026.** Sufficiently-large/eventual second-extremal diameter-2-critical classification around `M(n)=floor((n-1)^2/4)+1`. The false all-order 2019 strengthening is not assumed. The published 2024 `X_3` graph (`n=12,m=32`) remains a mandatory hostile control. Erdős #742 is preserved but is not the live optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_Z1_ZERO_BUFFER_REVERSE_COLLAPSE_2026_09_19`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `19244c3f44f082c51efb7463f1dc6aeca8a0026d`

LAST VERIFIED RESULT: `In the repaired rigid one-code z=1 common-buffer branch at zero buffer slack g=p, the reverse Orientation-B channel is actually empty. Each gamma-d matched crossing endpoint q has exactly one X-neighbour, so its tight mate q' of gamma-code bar-d is adjacent to the other x-1 X vertices. Since the buffer b is complete to X and x>=3, |N(b) cap N(q')|>=x-1>=2, contradicting the singleton requirement for every reverse certificate. Thus R_B=empty, r_B=0 and the reverse deficit is graph-forced to d=p, not the scalar-cheapest d=0. With g=p the matched Orientation-A channel is also empty because X-codes avoid d,bar-d, so every one of the x=p+k buffer-X edges has an outside-U certificate. Hence ell=x, every core head forces a distinct physical W_0--U_o nonedge and H_core>=k, giving E_core>=k(p+k). Distinct outside witnesses satisfy m>=ceil(x/R_A), H_Y>=ym, L_Y>=y(1+m), E_W>=[x-km]_+, the local one-code pair floor S_P>=k(p+k)+y(1+m), and the rooted hole floor Z>=ka+p+y(1+m).`

UNPRESERVED WORK: `None. The reverse-collapse proof, strengthened physical witness/slack ledgers, pair-local floor and rooted residual feedback are preserved in project/research/post_ms/2026-09-19-zero-buffer-reverse-collapse-v1/ZERO_BUFFER_REVERSE_COLLAPSE.md.`

DEFERRED ADMIN: `README remains lower-frequency; refresh it at the next daily adversarial audit/reviewer checkpoint if this repaired common-buffer line survives. Do not churn CI merely for bookkeeping.`

NEXT ACTION: `Stay in the reverse-collapsed zero-buffer branch. Retain the distinct outside-witness count m and combine the new pair-local floor S_P>=k(p+k)+y(1+m) with exact Ccap_P, (ONE-P) and (CROWD), then combine the resulting restriction with the exact rooted residual q+E_U bound. Symbolically optimize the one-dimensional m tradeoff before any finite scan. If a thin parameter strip survives, use the code-purity classes of the outside witnesses and raw criticality on the forced core--outside nonedges/nearby edges. Do not move to z=2 until this branch is exhausted.`

## Mandatory audit reconciliation

Before forward mathematics this run, the live `CURRENT_STATE.md`, root `README.md`, recent commits, the 19 September daily red-team audit/handoff, the source-premise repair, and the independent actual-D2C rigid Hall/pair-capacity regression were reread.

The audit priority order remains binding and no departure was made:

1. the distinct physical beta-source premise has a direct raw-rooted-criticality proof;
2. selected `(source,coordinate)` uniqueness is justified at the selected-representative level for a unique physical P--U obligation;
3. the principal `B_beta` lower bounds count those same selected physical obligations;
4. the finite source-tuple theorem is still not promoted as unconditional graph-level closure;
5. the independent actual-graph regression reaches rooted residual identities, tight codes, selected slots, Hall cuts and exact pair-local `Ccap_P`, records zero graph/formula mismatches, and permanently includes `X_3`;
6. no actual rigid complete Hall cut with `x>=3` has yet been found, so the live one-code results remain conditional hand implications of the rigid hypotheses;
7. exact `Ccap_P`, `(ONE-P)` and `(CROWD)` remain load-bearing;
8. the four-exception gate remains subordinate and the mixed `{4,5}` selected-excess ladder stays closed.

The earlier matched-only buffer-X theorem remains withdrawn. Only the corrected matched-or-outside criticality theorem may be used.

## Mandatory negative control

`X_3`: `n=12`, `m=32>M(12)=31`, canonical root `a=3,b=8,p=4,u=0,Q=12,r=f=delta=0`. The live common-buffer branch requires a nonempty `U_-=W_0 dotcup {b}` with `k>0`, so the new theorem does not exclude or misclassify `X_3`.

## Retained common-buffer setup

The live zero-buffer branch has:

- complete rigid cut `X--Y`, `x=|X|>=3`, `y=|Y|>0`;
- every Y-source of code `d`; X has neither code `d` nor `bar d`;
- `g=p`, `k=x-p>0`;
- `U_-=W_0 dotcup {b}`, `|W_0|=k`, `e(G[U_-])=0`;
- `epsilon_b=0`, hence `b--X` and `b--U_o` are complete;
- exact core identity `E_core=k(p+k-1)+H_core`, where `H_core=e_bar(W_0,U_o)`;
- graph-fixed partition `X=H_M dotcup H_0`, `|H_M|=p`, `|H_0|=k`.

The corrected criticality alternatives for `b x` are:

- A: `b z in E`, `x z notin E`, `N(x) cap N(z)={b}`;
- B: `x z in E`, `b z notin E`, `N(b) cap N(z)={x}`.

A is either matched with `gamma(z)=c(x)` or outside unmatched with `c(z)=bar c(x)` and z anticomplete to Y. B, if possible, uses a matched endpoint of gamma-code `bar d`.

## New reverse-channel collapse

For each tight fibre `{q,q'}` with

`gamma(q)=d`, `gamma(q')=bar d`,

the matched crossing layer and `X--Y` completeness force

> `N_X(q)={h_M(q)}`.

Tight-fibre transversality therefore gives

> `N_X(q')=X\{h_M(q)}`,
>
> `d_X(q')=x-1`.

Since b is complete to X,

> `|N(b) cap N(q')|>=x-1>=2`.

Thus q' cannot satisfy the reverse singleton condition `N(b) cap N(q')={x_0}` for any X-head. Therefore

> `R_B=emptyset`, `r_B=0`, `d=p`.                         `(REV-COLLAPSE)`

The previous scalar observation that the score floor is minimized at `d=0` remains arithmetically true, but that minimizer is not graph-realizable.

## Full outside certification

Every buffer-X edge must now use Orientation A. At `g=p`, every matched gamma code is `d` or `bar d`, while no X-source has either code. Therefore the matched-A channel is also empty.

For every `x in X` there is an outside witness `z_x in U_o` with

> `b z_x in E`, `x z_x notin E`,
>
> `N(x) cap N(z_x)={b}`,
>
> `c(z_x)=bar c(x)`,
>
> z_x anticomplete to Y.

No witness injectivity is assumed. The exact selected incidence count is

> `ell=x=p+k`.                                             `(OUT-ALL)`

## Core-hole consequence

For `w in W_0` with core head `x=h(w)`, choose an outside certificate z_x. Since `w~x` and `N(x) cap N(z_x)={b}`, one must have `w z_x notin E`.

The W_0 endpoints are distinct, so these give k distinct physical missing pairs even if some z_x repeat. Therefore

> `H_core>=k`,                                             `(CORE-HOLE)`
>
> `E_core>=k(p+k)`.                                       `(CORE-NEW)`

In particular `H_core=0` is impossible. The first live stability layer starts at `H_core=k`.

## Distinct outside-witness demand

Let W be the distinct selected outside witnesses, `m=|W|`, and `t_z` the number of X-sources served by z. A physical z can serve only one X-code class because `c(z)=bar c(x)` for every source it serves.

Using the preserved aligned-code cap `n_c<=R_A=floor(R_code(C0)/2)` gives

> `t_z<=R_A`,
>
> `m>=ceil(x/R_A)`.                                       `(WIT-M)`

Every z is anticomplete to Y, hence

> `H_Y>=ym`,
>
> `L_Y>=y(1+m)`.                                          `(WIT-LY)`

For the outside-witness U-slack,

> `epsilon_z >= [t_z-k]_+`,
>
> `E_W>= [x-km]_+`.                                       `(WIT-E)`

## Strengthened score and pair-local ledgers

The total score satisfies

> `S>=k(p+k)+[x-km]_+`
> `   +max{phi(p),y(1+m)}`,                               `(S-REV0)`

with `m>=ceil(x/R_A)`.

For the distinguished one-code pair P itself,

> `S_P>=k(p+k)+y(1+m)`.                                   `(SP-REV0)`

The exact pair-local bill remains simultaneous:

> `2xy<=Ccap_P`,
>
> `Ccap_P=R_code(S_P)*(p+2S_P/(lambda+1))`,               `(CCAP-REV0)`

along with `(ONE-P)` and `(CROWD)`.

## Strengthened rooted residual ledger

The repaired physical A--U hole count

`Z>=k(a-1)+y+ym+ell`

and `(OUT-ALL)` give

> `Z>=ka+p+y(1+m)`.                                       `(Z-REV0)`

Also

> `E_U>=E0(m):=k(p+k)+[x-km]_+`.                          `(EU-REV0)`

Put

`D(m)=ka+p+y(1+m)-u(p-lambda)`.

Using `Z=u(p-lambda)+2q+E_U`, exact integer minimization gives

> `q+E_U>=E0(m)+ceil([D(m)-E0(m)]_+/2)`.                 `(QE-REV0)`

This is the rooted residual input to use alongside `(SP-REV0)` and `(CCAP-REV0)`.

## Preserved full-support comparator

The full-support `z=1,h=0` branch remains valid. Raw criticality still forces `nu=y`, and the corrected rooted triangle baseline remains `Q=p(p-1)+pu+e(G[U])`. The new common-buffer collapse does not weaken it.

## Immediate frontier

Do not blind-scan and do not move to `z=2` yet. The next coherent attack is:

1. keep m explicit;
2. combine `(SP-REV0)` with exact `Ccap_P`, `(ONE-P)`, `(CROWD)`;
3. combine that restriction with `(S-REV0)` and `(QE-REV0)`;
4. symbolically minimize the one-dimensional m tradeoff;
5. seek an explicit sufficiently-large exclusion threshold or a sharply described residual strip;
6. if a strip survives, analyze code classes and raw criticality around the forced k core--outside nonedges.
<!-- CURRENT-STATUS:END -->
