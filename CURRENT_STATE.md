# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_K2_GLOBAL_HY_SOURCE_COLLAPSE_2026_09_20`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `cf82fce1c4b72a0e57299c7f325260d36f23fa2c`; before forward mathematics this invocation reread CURRENT_STATE.md, README.md, the latest commits, the 20 September daily red-team audit, the pure-F0 Y-edge certificate note, the q=1 witness-split note, the raw same-code audit, the residual-one code repertoire, and the cross-code/private-spoke capacity notes. The audit gate remained binding throughout.

LAST VERIFIED RESULT: `Same-session hostile correction and strengthening. The predecessor F0--Y forward witness branch was invalid: for a U-source, a U-witness shares the root and cannot have singleton common neighbourhood with an A-head. The raw two-orientation lemma also needs triangle-edge scope; the high-Y F0--Y application satisfies it because H supplies the triangle. In fact the repair globalizes. At residual one, k=2, J2=empty, p>=3, every outside escape t in E=U\W_s satisfies d_H(t)>0 => d_Y(t)<=1. Reverse certification of an escape--Y edge forces a witness w in U_bar(d), X-anticomplete and full-private-support; every private spoke of w is then forced forward, contradicting any H-neighbour of the certified head. Hence Z_{H,E}+Z_{Y,E}>=(p-1)(p-2) on the exact low-k ray. The exact U identity z_A(t)+m_U(t)=p+epsilon_t gives E_E>=2M_U(E)+M_U(E,W_s)-2|E|. Oriented U--U source capacity then localizes all quadratic U-density to B0={w in U_bar(d):d_H(w)=0}. Raw private-spoke criticality forces every B0 vertex to be Y-anticomplete; B0 is independent. Consequently e(U)<=b(u-b)+2(u-b), so M_U>=(1/4-o(1))p^2. Optimizing the legitimate weighted subtotal E_U+Z+2M_U over eta=|B0|/p gives the exact floor 11-4sqrt(5)=2.055728... at eta=(3-sqrt(5))/2. Finally, exact A-side identities L_H=Z_{H,U}+|H|-2e(H) and L_Y=Z_{Y,U}-|Y|-2e(Y) show that low L_A requires e(H)+e(Y)>=(1/2-o(1))p^2. Dense Y compensation itself requires U_bar(d) witnesses outside B0; the remaining live problem is therefore an H-dense / Y-dense compensator tradeoff, not the superseded golden dual-witness endpoint.`

UNPRESERVED WORK: `None after atomic preservation. New notes in project/research/post_ms/2026-09-20-rigid-interface-direct-audit-v1/: ONE_CODE_R1_K2_F0_Y_FORWARD_BRANCH_CORRECTION.md, ONE_CODE_R1_K2_F0_HY_POLARIZATION.md, ONE_CODE_R1_K2_GLOBAL_ESCAPE_HY_POLARIZATION.md, ONE_CODE_R1_K2_ESCAPE_HOLE_SLACK_COUPLING.md, ONE_CODE_R1_K2_U_DENSITY_SOURCE_CAPACITY.md, ONE_CODE_R1_K2_BAR_D_SOURCE_COLLAPSE.md, ONE_CODE_R1_K2_WEIGHTED_SOURCE_COLLAPSE_OPTIMIZATION.md, and ONE_CODE_R1_K2_A_SIDE_COMPENSATOR_IDENTITY.md. No finite scan was used.`

DEFERRED ADMIN: `README remains deliberately at the independently audited public checkpoint. Do not promote the same-session H/Y polarization, source-collapse, quarter missing-U floor, weighted 11-4sqrt(5) bound, or A-side compensator identity before the next daily hostile audit. Historical notes remain preserved. The prior q=1 golden forward/reverse/dual witness split and W_min curve are explicitly superseded by ONE_CODE_R1_K2_F0_Y_FORWARD_BRANCH_CORRECTION.md because their forward U-witness branch violates the root common-neighbour condition.`

NEXT ACTION: `First independently hostile-replay the new global escape H/Y polarization, especially the reverse F0--Y witness location and the private-spoke orientation forced by X-anticompleteness. Then replay B0 Y-antichain, B0 independence, oriented U-source capacity and the 11-4sqrt(5) weighted optimization. If they survive, attack the unused A-side slack term through ONE_CODE_R1_K2_A_SIDE_COMPENSATOR_IDENTITY.md: split low-L_A survivors into Y-compensated (linear B+=U_bar(d)\B0 witness class) versus H-compensated (dense H) arms, and audit dense H--H private-foot certificates against the U support pattern forced by B0/B+. Keep exact currencies E_U,Z_X,Z_Y,2M_U,2L_A separate. Separately redirect y=o(p) through g0=p-y and the existing large-gap/rooted residual machinery.`

AUDIT GATE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed in later repair work; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans are diagnostics only; superseded evidence stays superseded; bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3.`

SECOND STRICT: `Provisional exact unloaded second-strict chain remains finite-order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. Pending hostile replay.`

R1 K2 LIVE CHAIN: `Residual-one common hub/beta fan -> exact low-k k=2 ray -> escape-class budgets -> cross-code complement localization -> compressed half barrier -> pure-F0 endpoint -> hostile correction of F0--Y forward witness -> global escape H/Y polarization -> exact A-hole floor -> hole/slack coupling -> U-density source localization -> B0 H/Y-antichain and independence -> quarter missing-U floor -> weighted source-collapse minimum 11-4sqrt(5) -> A-side compensator identity -> live H-dense / Y-dense compensation attack; y=o(p) remains a separate large-gap route.`

R1 K2 SUPERSESSION: `The predecessor claim that a typical F0--Y forward certificate can use an atypical U-witness is false because source and U-witness share the root. Consequently the q=1 F/R/B witness LP, tau<=rho=(sqrt(5)-1)/2 golden threshold, golden dual-role equality class, and the piecewise W_min(tau) formula are not part of the live chain. They remain historically preserved but are superseded. The corrected route is strictly stronger: global H/Y polarization and source collapse.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:
- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in later repair;
- `X_3` mandatory negative control;
- zero positive actual D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until hostile replay.

## Live structural chain

`audited rooted/Hall setup`
`-> rigid singleton-head implication`
`-> provisional unloaded second-strict finite-order closure`
`-> one-code residual-one common hub / beta fan`
`-> k=2 low-k/high-y escape`
`-> cross-code / private-coordinate compression`
`-> compressed half barrier`
`-> hostile F0--Y certificate replay`
`-> forward-U-witness branch invalidated`
`-> global escape H/Y polarization`
`-> exact located A-hole floor`
`-> escape hole/slack coupling`
`-> U-density localized to H-free U_bar(d)`
`-> B0 forced H/Y-anticomplete and independent`
`-> M_U >= (1/4-o(1))p^2`
`-> weighted subtotal >=11-4sqrt(5)`
`-> low A-slack requires half-quadratic H/Y internal compensation`
`-> live dense-H / dense-Y compensator attack`.

## Session ledger — current invocation

**Invocation start:** 20:00:09 BST.  
**Planned preservation cutoff:** 20:55:38 BST.

Substantive units completed:
1. reconciled CURRENT_STATE.md, README.md, live head `cf82fce...`, the newest certificate commits, and the 20 September daily red-team audit before forward mathematics;
2. hostile-replayed the predecessor F0--Y argument and found the root-common-neighbour error in the supposed forward U-witness branch; also corrected the raw orientation lemma to triangle-edge scope;
3. proved that typical F0--Y edges are reverse-only, localizing every reverse witness to `U_bar(d)\W_s`, X-anticomplete and adjacent to the residual hub;
4. replaced the invalid golden certificate capacity by the stronger reverse-only population bound `tau<=1/2+o(1)` and derived the half-density sparse anchor-matching normal form;
5. combined reverse witness X-anticompleteness with the full private-coordinate fan to prove the exact F0 polarization `d_H(t)>0 => d_Y(t)<=1`;
6. removed the F0 hypothesis and proved the same H/Y polarization for every outside escape `t in U\W_s`;
7. summed the polarization to obtain the exact low-ray located-hole block `Z_{H,E}+Z_{Y,E}>=(p-1)(p-2)`;
8. derived the exact per-U hole/slack identity and the coupling `E_E>=2M_U(E)+M_U(E,W_s)-2|E|`;
9. localized oriented U--U source capacity to complementary A-code classes and showed that only H-free `U_bar(d)` vertices can have linear source capacity;
10. re-audited their private spokes and proved every such B0 vertex is also Y-anticomplete;
11. proved B0 is independent and derived `e(U)<=b(u-b)+2(u-b)`, hence the universal `M_U>=(1/4-o(1))p^2` floor;
12. retained both global and B0-specific slack prices and exactly optimized `E_U+Z+2M_U`, obtaining `11-4sqrt(5)=2.055728...` at `eta=(3-sqrt(5))/2`;
13. derived exact H-side and Y-side A-slack identities and showed low `L_A` forces `e(H)+e(Y)>=(1/2-o(1))p^2`;
14. localized Y--Y compensation to `U_bar(d)` witnesses outside B0, exposing the B0/B+/dense-H three-way tradeoff;
15. preserved all corrections, strengthened the live structural chain, and explicitly marked the prior golden witness split as superseded rather than silently reusing it.

**Live unfinished line:** independently replay the global H/Y/source-collapse chain, then classify the dense-H compensation arm by raw H--H private-foot criticality and its compatibility with B0/B+ U-support. In parallel, price the dense-Y arm through B+ witness capacity. Keep the low-y `y=o(p)` route separate.

**Stop reason:** final preservation checkpoint after fifteen tightly connected mathematical/audit units. No mathematical blocker; the next dense-compensator attack is preserved precisely for the following invocation.