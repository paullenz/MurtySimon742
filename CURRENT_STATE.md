# Dense diameter-2-critical research - live current state

> **Active target - 19 September 2026.** The live problem is the sufficiently-large/eventual second-extremal D2C classification around `M(n)=floor((n-1)^2/4)+1`. The false all-order 2019 Dailly-Foucaud-Hansberg strengthening is not assumed. The published 2024 order-12, size-32 D2C graph is a mandatory hostile control. Murty-Simon / Erdos #742 remains preserved but is not the live optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `DAILY_RED_TEAM_AUDIT_GUARD_SEMANTICS_REPAIR_2026_09_19`

WORK MODE: `AUDIT`

INSPECTED PREDECESSOR: `4b4986be1efd3315714e3bf0341bfd6dc5082133`

LAST VERIFIED RESULT: No fatal mathematical contradiction was found in the current load-bearing rooted-witness/Hall/rigid-U-witness spine. The finite source-tuple capacity theorem, including its finite arbitrary-subset layer-cake deficit form, has been independently re-derived conditional on two explicit shared premises: distinct physical sources and global selected source-coordinate uniqueness. Those premises and the wider graph-to-constraint interface remain the principal end-to-end verification gap. The explicit `X_3` construction independently replays as a 12-vertex, 32-edge D2C graph. Repository audit has restored current reviewer navigation and protected red-team history; the README guard is repaired only to make protected prose matching case-insensitive, without removing any required token, link, or substantive historical check.

UNPRESERVED WORK: None.

DEFERRED ADMIN: Historical Git-LFS checkout warnings for seven legacy ZIP paths remain preserved and have not been altered because they are not blocking the live mathematics and touch frozen historical evidence.

NEXT ACTION: Verify distinct-source and selected source-coordinate uniqueness directly from the rooted criticality construction; build a graph-level regression including `X_3`; then attack the one-code rigid branch with the exact `Ccap_P`, `(ONE)` and `(CROWD)` formulas.

## Mathematical audit boundary

The preceding 24-hour interval was compared from `f2e85599491d9804084e9739e1cfcdc6cc088a29` to pre-audit head `0060acd80a376074486563bc537386fe130459d2`, spanning 216 commits. No global eventual second-extremal theorem is claimed.

Detailed audit: `project/research/post_ms/2026-09-19-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Independent source-tuple re-proof: `project/research/post_ms/2026-09-19-daily-red-team-audit-v1/INDEPENDENT_SOURCE_TUPLE_REPROOF.md`.

Process follow-up: `project/research/post_ms/2026-09-19-daily-red-team-audit-v1/FOLLOW_UP_PROCESS_REPAIR.md`.

Trust boundary:

- `X_3`: explicit graph property independently verified (`n=12`, `m=32`, diameter 2, every edge critical); paper-figure identification remains internally figure-based.
- Source-tuple/`FDPr`/finite layer-cake deficit: independently re-derived at abstract selected-system level, conditional on distinct-source and source-coordinate uniqueness premises.
- Rooted residual, Hall exact-cut/density, rigid code collapse and U-witness bills: hand/algebraically rechecked conditional on graph-theoretic rooted/certificate premises.
- Bounded-surplus four-exception gate: strongly supported, not fully independently re-derived here.
- End-to-end graph-to-constraint implementation: still missing.

Key rigid-cut formulas retained: `hk<=u`, `Z>=k[y+h(x-1)]`, for `g>=1`, `E_U>=k[y+h(g-1)]`; one-code `(ONE)` is `Ccap_P+L_Y>=y(p+x+k)`, and aligned crowding `(CROWD)` is `S_P>=y(3y-D0)`.

## Repository/README audit trail

Deterministic findings are preserved rather than relabelled as transient:

1. `35402717100` / `105785888407`: pre-audit status schema mismatch.
2. `35405621026` / `105794668021`: Markdown-bold `WORK MODE` parser defect; repaired without enlarging the allowed mode set.
3. `35405621004` / `105794668117`: valid README directory links rejected by N30 checker; repaired to accept existing file or directory targets.
4. `35406193034` / `105796333427`: N30 exposed protected direct fixed-order PDF links lost during README compression; restored.
5. `35406504877` / `105797249965`: README guard exposed five missing current general-theory reviewer links; restored.
6. `35406744831` / `105797951718`: README guard then failed solely because the protected phrase `Hostile coverage/integrity audit` had sentence capitalization while the required token was lower-case. The protected history and links were present. The guard now uses case-insensitive semantic matching for the same required tokens; no required content is removed.

N30 run `35406744795` passed on the restored README. Status synchronization had already passed at reconciled run `35406504886`. This checkpoint must itself clear the relevant guards before process hygiene is considered closed.

## Next programme and stop/pivot criteria

1. Re-derive the two source-tuple premises directly from rooted criticality/selected-certificate definitions. Any reuse of `(source,coordinate)`, duplicate physical source, or ambiguity blocks downstream source-tuple applications.
2. Build graph-level rooted/Hall/rigid-witness regression on realizable D2C graphs including `X_3`; first mismatch is a proof blocker.
3. Combine exact pair-local `Ccap_P`, `(ONE)` and `(CROWD)` before any further global scalar relaxation. Seek contradiction or classify equality geometry.
4. Feed any surviving geometry through forced `q,E_U,f` and the rooted residual identities rather than accumulating weaker scalar bounds.
5. Revisit the four-exception gate only if it becomes load-bearing.

Keep the mixed `{4,5}` ladder closed. Do not optimize for first-proof priority on Erdos #742.
<!-- CURRENT-STATUS:END -->
