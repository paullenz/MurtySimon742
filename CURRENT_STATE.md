# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `SECOND_STRICT_Y1_LAST_X3_TAIL_2026_09_20`

WORK MODE: `MATH_AFTER_AUDIT_GATE`

INSPECTED_PREDECESSOR_HEAD: `338210b5066502d22d332e3ace2bab2627382a20`

LATEST_NEW_COMMITS: `e1a7228aa338a528711e57bc888e298dae26d092` (two-X-hole forward-routing physical surcharge), `98a10ab51cb80980f6cce10cbdbf440dd784cd87` (bounded-resource x>=14 closure), `ab3e45169516c758fa55fb5e92cee07281dacd3c` (complete two-X-hole x>=16 closure), `9ccf6be0f64dd5ad888cd84bdd476988a8cb50ec` (two-X-hole n<=816 finite-order bound), `9cbfb9123ad90ef866768b996a5df88e4417a318` (hostile margin/slot audit), `7d9bdb5ee6956221208788883154a046077c1c63` / `0f1a19d2f0af16270ef4d9f4776bcb2da602da43` (two-X-hole checker/replay), `0531c0f955ab820be05d99dd626fa3f9055ef97b` (exceptional-Fz k=1 scaling correction), `a1e741884104179489eff7ce80a229bbb07228b9` (repair y1 diagnostic physical gate), `8e554fe15df7f51661f3eb1bcb96502be3a78535` (all large-head mixed y1 n<=33), `6899fd2b02043af8e1888e561d521955769df57e` (corrected y1 replay), `38dad29b91410d0ab679041e61283ec5568f754f` (small-head scope), `9ebef81ca63161abacc622085367bb7813632439` (x=4 singleton-support n<=19), `dd7988d9ba5a44f21361c3af49f348a664f2a774` (orientation polarization; eliminates x=4 support corner).

LAST VERIFIED/AUDITED BASE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; the raw same-code criticality theorem and ordered (source,witness) injection were independently re-derived and passed; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans remain diagnostics only; and bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3. The zero-positive-fixture rigid-cut interface remains the dominant global audit risk.`

FIRST_STRICT_STATUS: `Same-session candidate closure remains provisional: Type F is impossible and the remaining all-R first-strict arm collapses to the already-closed maximal-m1 all-R contradiction, giving unloaded floor epsilon_b>=p-g+2. Independent daily replay is still required.`

SECOND_STRICT_SPLIT: `At epsilon_b=p-g+2 the exact physical split is (h_X,h_o)=(2,0),(1,1),(0,2). The (0,2) arm is internally closed. Mixed (1,1), y>=2 is internally closed. The two-X-hole (2,0) arm is now a same-session candidate finite-order branch n<=816. Mixed y=1 is finite-order for x>=4, leaving only x=3 unbounded in scope.`

TWO_X_HOLE_RESULT: `Physical edge localization plus the represented-hole routing theorem gives forced forward traffic F_C=s_C[m_C-2]_+. Each such witness creates one extra X--U_o hole and one extra core--U_o hole, yielding E_core>=x(p+x-1)+L+F_H, L_X>=x(p+x-y)-(x-2)+L+F_H-2e_X, q<=(x+1)omega-L-F_H+M_H, and E(U_o)>=[omega(p-x)+2L+2F_H]_+. The exact positive-combination obstruction then gives Phi<=11M_H-8F_H. Per hole class 11m-8F_C<=3m+16, which closes x>=16 analytically. The x<=15 tail satisfies p+omega<=196, y<=392 and n<=816. Therefore the exact core-saturated two-X-hole branch is internally impossible for n>=817.`

TWO_X_HOLE_AUDIT: `A hostile same-session check verified that internal X-density does not lower the baseline r>=x+y because the complete unequal-code X--Y cut forces r_z>=1 at every A-vertex. This validates the 2M/3M and forward-surcharge margin coefficients. An independent checker replayed 3,608,425 exact square identities; finite replay is audit support only.`

Y1_DIAGNOSTIC_CORRECTION: `The prior “unbounded exceptional-Fz k=1 family” was a feasibility artefact. The diagnostic admitted negative exact buffer slack epsilon_b=p-g+2 even though root maximality forces epsilon_b>=0. That claim is withdrawn. The repaired checker rejects these rows. On the original box survivors fall from historical R/Fz0/Fx/Fz1 = 2821/2840/691/2276 to 86/86/17/38, and the same counts persist on an expanded p<=30,x<=60,omega<=50 box. Counts remain diagnostic only.`

Y1_LARGE_HEAD_RESULT: `For every classified p>=2,x>=5 mixed-y1 R/Fz/Fx geometry, combine g<=p+2 with the exact concave rooted margin in omega. R and no-exception Fz require p+k<=6; exceptional Fz also requires p+k<=6; Fx requires p+k<=5. Hence R/Fz have p<=5,x<=8,omega<=11,n<=33 and Fx has x<=7,omega<=9,n<=28. Thus the whole classified large-head mixed-y1 arm is internally impossible for n>=34.`

Y1_X4_RESULT: `The p=1 case is impossible at the rigid interface because a one-bit code cube has only d and bar d while every X-code must be neither. For x=4, if |S_0|=1 the R/Fx/Fz proofs transfer without using x>=5; exact rooted-margin optimization gives p<=3 and n<=19. More importantly, dd7988... strengthens the old exceptional-capacity count: for a fixed coordinate j, adjacency z_0q_j globally fixes whether every obligation at j must be forward or reverse. If H>=2, any reverse-only coordinate is impossible because the fixed pair (q_j,z_0) cannot have two different singleton heads; hence every coordinate is forward-only, and a fixed pair (x,z_0) can support at most one such coordinate. Therefore H>=2 => |S_0|=1. Since x=4 has H>=2, the apparent H=2,|S_0|=2 corner is empty. Consequently every mixed-y1 x=4 survivor has n<=19.`

Y1_REMAINING_TAIL: `The only mixed-y1 head count not yet structurally bounded is x=3 (with p>=2). If its outside-certified head count H>=2, the new orientation-polarization theorem already forces |S_0|=1, so much of the R/Fx/Fz machinery should transfer. The genuinely exceptional case is x=3,H=1, where one buffer edge is outside-certified Type R and the other uses the single exceptional reverse channel; this is the next raw-criticality target.`

DEPENDENCY_CLEANUP: `Do not return to the withdrawn k=1 scaling attack. The two-X-hole, large-head y1 and x=4 y1 arms are same-session candidate finite-order branches, not live asymptotic families. README remains at the independently audited checkpoint until the daily red-team replay. Exact pair-local machinery is still mandatory where invoked, but it is not currently needed to close these particular scalar tails.`

NEXT_ACTION: `First hostile-replay (i) two-X-hole forward-route hole disjointness / Phi<=11M-8F_H, (ii) y1 buffer nonnegativity and rooted-margin maximization, and (iii) the new coordinate-orientation polarization dd7988.... If they pass, attack the sole remaining mixed-y1 x=3 tail. Split H>=2 (then |S_0|=1 and transfer the R/Fx/Fz proof) from H=1. In H=1, reconstruct the one outside-certified Type-R head and one exceptional reverse buffer head directly from raw criticality; seek explicit bounds on p and omega. If x=3 is finite-order, the exact unloaded second-strict layer is asymptotically exhausted and the next eventual branch should be higher buffer defect / loaded buffer. Keep the zero-positive-rigid-cut interface gap explicit throughout.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:

- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed;
- `X_3` mandatory negative control;
- zero positive actual-D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until independent adversarial replay.

## Live structural chain

`audited rigid one-code setup`
`-> provisional first-strict closure`
`-> unloaded floor epsilon_b>=p-g+2`
`-> exact second-strict split`
`-> (0,2) empty`
`-> mixed (1,1), y>=2 internally closed`
`-> mixed y=1 x>=5 finite-order n<=33`
`-> x=4 orientation polarization -> |S_0|=1 -> finite-order n<=19`
`-> remaining mixed y1 tail: x=3 only`
`|| (2,0) two-X-hole -> forward physical surcharge -> x>=16 closed -> full tail n<=816.`

## Session ledger — current invocation

**Invocation start:** 07:27:41 BST.  
**Planned preservation cutoff:** 07:55:38 BST.

Substantive units completed:

1. reread `CURRENT_STATE.md`, README, daily red-team audit and latest commits; reconciled stale state against the newer two-X-hole packages;
2. hostile-checked class routing and extracted `F_C=s_C[m_C-2]_+` forced forward traffic;
3. proved each forward route creates two new located physical holes and corresponding score/rooted surcharges;
4. derived the extreme-radius Hamming-slot floor for unbounded distinguished hole classes;
5. analytically closed bounded `M_H<=8` two-X-hole resource for x>=14;
6. optimized the forward surcharge per class and analytically closed the entire two-X-hole arm for x>=16;
7. converted the x<=15 two-X-hole tail into the global finite-order bound n<=816;
8. hostile-audited the represented-resource margin accounting against the local Hamming theorem and found no missing rooted-slot rebate;
9. built/replayed an independent two-X-hole checker (3,608,425 exact square identities);
10. discovered a genuine feasibility bug in the y1 diagnostic: negative exact buffer slack had been admitted;
11. proved the purported exceptional-Fz k=1 unbounded family is actually finite-order once epsilon_b>=0 is imposed;
12. repaired the y1 diagnostic and independently replayed a larger parameter box;
13. derived an analytic all-case large-head y1 finite-order theorem n<=33;
14. eliminated p=1 at the rigid code interface and localized the residual y1 work to x=3,4;
15. transferred the singleton-support R/Fx/Fz classification to x=4 and derived n<=19;
16. strengthened the mixed exceptional-capacity theorem using coordinate-wise orientation polarization, eliminating the old H=2,|S_0|=2 x=4 corner and leaving x=3 as the sole mixed-y1 head tail.

**Live unfinished line:** reconstruct and bound the x=3 mixed-y1 tail, especially H=1. If finite-order, the exact unloaded second-strict layer is asymptotically exhausted.

**Stop reason:** preservation phase reached after using the shortened window through sixteen substantive units, including a material diagnostic correction and two asymptotic branch compressions. No mathematical blocker and no clean-checkpoint early stop.