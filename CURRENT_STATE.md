# Dense diameter-2-critical research - live current state

> **Active target - 19 September 2026.** The live problem is the sufficiently-large/eventual second-extremal D2C classification around `M(n)=floor((n-1)^2/4)+1`. The false all-order 2019 Dailly-Foucaud-Hansberg strengthening is not assumed. The published 2024 order-12, size-32 D2C graph `X_3` is a mandatory hostile control. Murty-Simon / Erdos #742 remains preserved but is not the live optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_Z1_ZERO_BUFFER_LOCAL_PAIR_2026_09_19`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `14af481cf888c6cb3a8c22f5a51a01c75474a281`

LAST VERIFIED RESULT: `After correcting the common-buffer b--X criticality orientation, the zero-buffer subbranch g=p forces all k=x-p non-reverse certificates into outside U_o, not matched feet. Keeping the complementary-pair slack local gives exact traffic P_P=yp, C_P=yk, t_P=xy and an exact directed-A/U score floor s_AU. On the bounded diagnostic, s_AU strictly raises the local S_P floor in 5,280 zero-buffer parameter branches, but exact Ccap/CROWD arithmetic adds no further scalar exclusions beyond the repaired geometric floor. The next gain must therefore come from simultaneous near-equality structure and residual coupling, not another coarse score minimum.`

UNPRESERVED WORK: `None. The orientation repair, replacement theorem, zero-buffer local-pair theorem and both arithmetic diagnostics are preserved under project/research/post_ms/2026-09-19-common-buffer-criticality-repair-v1/.`

DEFERRED ADMIN: `README remains lower-frequency. Do not spend the mathematics cadence on CI/reviewer packaging unless a repository integrity failure appears. Refresh reviewer-facing status at the next daily audit if this repaired branch survives.`

NEXT ACTION: `Stay on the zero-buffer g=p common-buffer branch. Classify simultaneous near equality in outside-witness reuse m=m0, L_Y=y(1+m), core slack E_bar=E_core or the exact extra amount forced by s_AU, directed A/U capacity, exact crossing Ccap, and QE-CB residual feedback. Convert equality into physical degree/code structure of W0 and the outside witnesses. If that does not close, then treat p-g>0 with the matched-cap/outside-hole dichotomy, then r>0. Do not move to z=2.`

## Audit reconciliation

Before forward mathematics, `CURRENT_STATE.md`, `README.md`, recent commits, the 19 September daily red-team audit/handoff, source-premise repair and actual-D2C Hall/pair regression were reread. There is no departure from the audit priority order. The two source-tuple premises remain repaired at the raw/selected interface; the finite source-tuple theorem is not treated as unconditional graph closure; the actual-D2C regression still records zero graph/formula mismatches and retains `X_3`; no positive actual graph realizes the rigid complete Hall cut with `x>=3`; exact `Ccap_P`, `(ONE-P)` and `(CROWD)` remain load-bearing. The four-exception gate is subordinate and the mixed `{4,5}` ladder stays closed.

The previous handoff's matched-only common-buffer attack was **not** followed because raw rereading exposed an impossible criticality orientation. Continuing it would have violated the audit rule against building on a weakened premise.

## Mandatory negative control

`X_3`: `n=12`, `m=32>M(12)=31`, canonical root `a=3,b=8,p=4,u=0,Q=12,r=f=delta=0`. The live common-buffer mechanisms require `k+1<=u`, so `X_3` is untouched.

## Corrected criticality interface

The invalid predecessor statement was in

`project/research/post_ms/2026-09-19-common-buffer-criticality-v1/ANTICOMPLETE_BUFFER_X_EDGE_TRICHOTOMY.md`.

Its old Orientation I combined `b z` nonadjacency with `N(x) cap N(z)={b}`, which is impossible. The corrected triangle-edge orientations for `b x` are

`(A)` `b z in E`, `x z notin E`, `N(x) cap N(z)={b}`;

`(B)` `x z in E`, `b z notin E`, `N(b) cap N(z)={x}`.

Orientation B remains matched-only with `gamma(z)=bar d`, and at most g X-heads can use it. Hence at least `k=x-g` buffer--X edges use A. Corrected A is **matched-or-outside**:

- matched foot: `gamma(z)=c(x)`;
- `z in U_o`: `c(z)=bar c(x)` and z is anticomplete to Y.

The old unconditional `>=k` matched-foot demand is withdrawn.

Survives: the predecessor common-buffer score theorem, source--buffer triangle-freeness for `r>0`, rectangular Y-hole payment, the z=1 arithmetic checker, and the full-support `nu=y` theorem. A hostile reread found no analogous orientation error in `OMITTED_PAIR_NONEDGE_FORCING.md`.

## Matched capacity versus outside load

For an above-threshold candidate with `lambda>=0`, put

`D0=5p+5u-3lambda-2`,

`R_code(C0)=floor((D0+sqrt(D0^2+12C0))/3)`,

`R_A=floor(R_code(C0)/2)`.

Aligned-code self-pricing gives `n_c<=R_A` for every A-code class. Matched Orientation-A feet for X-sources live only in the `p-g` non-P gamma fibres, exposing at most `2(p-g)` usable gamma codes. Therefore

> `q_M<=2(p-g)R_A`,
>
> `ell >= [k-2(p-g)R_A]_+`.                              `(OUT-FORCE)`

If `epsilon_b=0`, then `g=p`, so the matched capacity is zero and

> `ell=k=x-p`.                                             `(ZERO-OUT)`

Thus zero buffer slack forces the entire non-reverse load into outside-U witnesses.

A fixed outside witness serves only one complementary X-code class and hence at most `R_A` sources. If m distinct outside witnesses are used,

> `m>=ceil(ell/R_A)`,
>
> `H_Y>=ym`,
>
> `E_W>= [ell-(x-p)m]_+`.

The exact r=0 source slack gives

> `L_Y=y(p-g+1)+H_Y`.

The repaired total score floor is

> `S>=E_core+(p-g)+E_W+max{phi(g),y(p-g+1+m)}`,

with `E_core=k(p+k-2)`.

The pair-P local floor is

> `S_P>=E_core+(p-g)+y(p-g+1+m)`.                         `(SP-LOCAL)`

## Zero-buffer exact local-pair theorem

Now specialize to `g=p`, `k=x-p`, and let

> `m0=ceil(k/R_A)`.

The geometry gives

> `A:=L_Y floor = y(1+m0)`,
>
> `B:=E_bar floor = E_core=k(p+k-2)`,
>
> `s_geom=A+B`.

Traffic on the one-code pair is exact:

> `P_P=yp`,
>
> `C_P=yk`,
>
> `t_P=xy=y(p+k)`.                                        `(TRAFFIC)`

Therefore exact crossing capacity requires

> `2xy<=R_code(S_P)[p+2S_P/(lambda+1)]`.                 `(CCAP-Z)`

The directed A/U capacity is

> `(lambda+1)yk <= (k+1)L_Y+yE_bar`.                     `(AU-Z)`

Minimizing `L_Y+E_bar` over the integer floors A,B gives the exact useful-pair minimum

> `s_AU=A+B`
> ` +ceil([ (lambda+1)yk-(k+1)A-yB ]_+ / max(k+1,y))`.   `(SAU)`

If `y>k+1`, any required extra useful slack is cheapest in `E_bar`; if `k+1>y`, it is cheapest in `L_Y`.

Define `s_cap` as the least integer s satisfying

> `R_code(s)[p(lambda+1)+2s]>=2xy(lambda+1)`,

and

> `s_crowd=[y(3y-D0)]_+`.

Then

> `S_P>=s_*:=max{s_geom,s_AU,s_cap,s_crowd}`.            `(PAIR-STAR)`

This is the compact exact local feasibility envelope requested by the audit. Only after forming it is the global ceiling `S_P<=C0` applied.

## Rooted residual feedback

The corrected physical A--U holes satisfy

> `Z>=k(a-1)+y+ym+ell`.

At zero buffer slack, `ell=k`, hence

> `Z>=ka+y(1+m)`.                                         `(Z-ZERO)`

With minimal m0 and `E_U>=E_core`, put

`D_zero=ka+y(1+m0)-u(p-lambda)`.

Then

> `q+E_U>=E_core+ceil([D_zero-Ecore]_+/2)`.              `(QE-ZERO)`

This must be enforced simultaneously with `(PAIR-STAR)`; pair saturation cannot be optimized independently of rooted defect.

## Diagnostic evidence

`check_common_buffer_x_edge_repair.py`:

- 16,065 exhaustive small convex-reuse checks; minimum margin zero; failures zero;
- 345,219 abstract r=0 equality g-branches passing the predecessor scalar floor;
- 17,174 with forced outside load, all zero-buffer `g=p` in this bounded box;
- 113 zero-buffer branches rejected by the repaired geometric Y-hole floor; 17,061 remain.

`check_zero_buffer_local_pair_feasibility.py`:

- independently brute-forces the integer optimization behind `(SAU)`;
- among the 17,174 zero-buffer predecessor branches, `(SAU)` strictly raises the local pair floor above `s_geom` in 5,280;
- exact `s_cap` and `s_crowd` do not strictly beat `s_geom` on this bounded box;
- the combined local pair envelope creates no additional total-C0 rejection beyond the same 113 geometric rejections.

These are diagnostics, not graph counts. The negative numerical result is informative: another scalar stacking step is unlikely to close the branch.

## Preserved full-support comparator

The full-support `z=1,h=0` branch remains valid and more expensive. Raw criticality forces every omitted source--`U_-` pair to be a nonedge (`nu=y`), and the rooted triangle baseline remains

`Q=p(p-1)+pu+e(G[U])`.

No current repair weakens it.

## Immediate frontier

The next attack is structural equality, not a larger scan:

1. assume a zero-buffer survivor is close to `(PAIR-STAR)` and `(QE-ZERO)`;
2. classify equality in `m=m0`, the Y-hole rectangle, core slack and `(AU-Z)`;
3. use the coefficient direction in `(SAU)` to identify whether extra slack must be on W0 or Y;
4. translate that into exact degrees/neighbourhoods of W0 and the outside witnesses;
5. test those physical neighborhoods directly against D2C criticality and the exact pair-capacity witness geometry;
6. only if zero-buffer survives, move to `p-g>0`; then r>0; only then z=2.
<!-- CURRENT-STATUS:END -->
