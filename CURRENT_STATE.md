# Dense diameter-2-critical research - live current state

> **Active target - 19 September 2026.** The live problem is the sufficiently-large/eventual second-extremal D2C classification around `M(n)=floor((n-1)^2/4)+1`. The false all-order 2019 Dailly-Foucaud-Hansberg strengthening is not assumed. The published 2024 order-12, size-32 D2C graph is a mandatory hostile control. Murty-Simon / Erdos #742 remains preserved but is not the live optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_Z1_SOURCE_VISIBILITY_PURIFIED_2026_09_19`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `c0ddbaba3eebd0af40f0df516494d6b5cbe3e944`

LAST VERIFIED RESULT: The 19 September audit-mandated source-premise repair and actual-graph regression remain intact with zero recorded graph/formula mismatches and `X_3` retained as a mandatory hostile control. A hostile reread of the just-preserved z=1 cap/equality package exposed a stronger graph-level theorem. If a complementary U-witness is used on the rigid complete `X--Y` cut, it has exactly one X-neighbour; since every Y-source sees all of X, that used witness is sterile for every same-code singleton-common-neighbour certificate whose head is outside X. Consequently all same-code auxiliary traffic is forced onto U_- vertices unused by the crossing support. In general, if `W` is the crossing-witness union and `B_U=U_-\W`, then `W` is independent, `B_U` is a vertex cover of `G[U_-]`, every active buffer has `d_X=0`, and `e(Y)+e(Y,U_d)+e(G[U_-])<=y|B_U|`. In z=1 full support there are no buffers, so graph-level same-code localization forces `e(Y)=e(Y,U_d)=e(G[U_-])=0` outright. Writing `h=y-rho`, the corrected full-support normal form collapses to `E_- >= B-y+h`, `Z>=Z0-y+h`, and `L_Y>=y(p-g+u_+)+h`. Hence with `Ebase=[B-y]_+` and `Dbase=Z0-y-u(p-lambda)`, the exact residual floor is `q+E_U>=Ebase+ceil([Dbase-Ebase]_+/2)`. At minimal `h=0`, the full-support witness pattern is also classified: every source omits exactly one of the k+1 U-witnesses, and full support permits exactly two omission types; those two omitted vertices share one X-head, while all other U-witness heads are distinct. Independent replay checked 88,200 residual instances and 2,083 qualifying omission maps with zero mismatches. The prior finite-edge-cap full-support clique equality model is therefore empty: the preceding cap theorem remains valid but is superseded in full support by source-visibility sterility.

UNPRESERVED WORK: None after the accompanying commit. The source-visibility theorem, general buffer localization, purified full-support normal form/residual theorem, exact crossing counts, omission-pair classification, checker and audit summary are preserved in `project/research/post_ms/2026-09-19-one-code-source-visibility-v1/`.

DEFERRED ADMIN: Root `README.md` has not yet been expanded with the one-code reservoir / z=1 closure / source-visibility packages. Refresh it at the next reviewer-facing status or daily-audit checkpoint. Historical Git-LFS warnings for seven legacy ZIP paths remain preserved and non-blocking.

NEXT ACTION: Stay on z=1 until the two remaining minimal geometries are exhausted. First attack the new full-support **two-omission / duplicated-X-head** model with exact pair capacity, individual U-slack equality, rooted residual `delta=r-f`, and rooted triangle count `Q`; in parallel compare it directly with the unloaded common-buffer model (one omission type). Try to prove that the duplicated-head pair forces extra slack/defect or a forbidden local criticality pattern. Only after this exact equality/stability analysis should the run move to z=2. For any later z>=2 work, use source-visibility buffer localization as the organizing theorem: used crossing witnesses are sterile and all auxiliary same-code traffic lives on unused buffers. Keep the mixed `{4,5}` ladder closed, the four-exception gate subordinate, first-proof priority on Erdos #742 inactive, and `X_3` hostile.

## Mandatory audit reconciliation

Latest daily audit:

- `project/research/post_ms/2026-09-19-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`
- `project/research/post_ms/2026-09-19-daily-red-team-audit-v1/INDEPENDENT_SOURCE_TUPLE_REPROOF.md`

The audit priority order was: independently verify the two source-tuple premises; construct actual-graph regression through rooted/Hall/pair-capacity objects with `X_3`; only then push exact pair-local one-code geometry and feed survivors back into the residual ledger. Those upstream obligations were re-read and remained satisfied before this session. The source-visibility move is not a departure from the audit plan: it is an adversarial repair/sharpening of the authorized one-code branch using a graph-level lemma already inside the regressed interface.

## Upstream trust boundary

### Source-tuple premises

Package: `project/research/post_ms/2026-09-19-source-premise-graph-audit-v1/`.

P1 remains a direct raw-criticality fact at the correct beta orientation: one physical `(x,y)` cannot beta-certify two target fibres because fixed `N(x) cap N(y)` cannot equal two distinct singleton tight endpoints. P2 remains selected-representative uniqueness for one physical rooted B-edge/source-coordinate obligation, not raw-witness uniqueness. The finite source-tuple capacity theorem is independently re-derived only conditional on these two named graph-to-selected premises.

### Actual graph -> Hall / pair-capacity regression

Package: `project/research/post_ms/2026-09-19-rigid-graph-regression-v1/`.

Recorded coverage remains 3,540 root-policy instances, 114 qualified pair/slack instances, 147 exact `Ccap_P` checks, 96 one-sided-pair checks, 36 exact Hall-cut decompositions, zero mismatches, with a genuine matched-B positive control. No actual rigid complete Hall cut with `x>=3` occurs in the bounded corpus, so the rigid one-code results remain conditional hand deductions from graph-regressed upstream ingredients.

### Mandatory `X_3`

The explicit fixture verifies `n=12`, `m=32>M(12)=31`, diameter two, every edge critical, canonical root `a=3,b=8,p=4,u=0`, and `Q=12,r=f=delta=0`. The present mechanism requires unmatched U-witnesses and is inactive on this canonical root.

## Corrected z=1 lineage

Load-bearing corrected predecessor:

`project/research/post_ms/2026-09-19-one-code-reservoir-v1/ONE_CODE_Z1_NEAR_SATURATION_V2.md`.

The first v1 proof that all internal Y-edges selected the A/U channel remains invalid. The v2 variables and correction remain the safe selected-traffic bookkeeping.

Immediately preceding package:

`project/research/post_ms/2026-09-19-one-code-z1-closure-v1/`.

Its finite edge cap, cap-aware residual elimination and common-buffer `+x` surcharge are valid deductions. However its hypothetical full-support cap-binding clique equality model is now proved empty, because full-support crossing witnesses cannot participate in the same-code certificates required by any U_- edge. In full support, the stronger source-visibility package below supersedes the cap-based scalar treatment.

## Source-visibility / buffer theorem

Package:

`project/research/post_ms/2026-09-19-one-code-source-visibility-v1/`.

Let `W subseteq U_-` be the union of U-witnesses used by crossing certificates and put `B_U=U_-\W`, `r=|B_U|`.

Every `w in W` has exactly one X-neighbour. Since each Y-source is adjacent to all of X, such a used witness cannot appear in any same-code source/witness certificate whose prescribed head lies in `Y union U` rather than X. Combining this with graph-level same-code localization gives:

- witnesses for edges inside Y or `Y--U_d` lie in `B_U`;
- every U_- edge has its same-code certificate sourced in `B_U`;
- `W` is independent;
- `B_U` is a vertex cover of `G[U_-]`;
- any buffer used by an auxiliary certificate has `d_X=0`;
- `e(Y)+e(Y,U_d)+e(G[U_-]) <= yr`;
- `e(G[U_-]) <= r|W|+binom(r,2)`.

This bypasses selected-channel ambiguity: one independently guaranteed same-code certificate is chosen for every relevant graph edge.

## Purified z=1 full support

For z=1 full support, `u_-=k+1` and `B_U=emptyset`. Hence

`e(Y)=e(Y,U_d)=e(G[U_-])=0`.

Let `rho` be the crossing-hole count and put `h=y-rho`. With

`B=(k+1)(p+k-1)`,

`Z0=(k+1)(a-1)`,

`D0=Z0-u(p-lambda)`,

the exact structural floors are

`E_- >= B-y+h`,

`Z >= Z0-y+h`,

`L_Y >= y(p-g+u_+)+h`.

Thus

`S >= max(phi(g),y(p-g+u_+)+h)+[B-y+h]_+`,

and safely

`S >= max(phi(g),y(p-g))+[B-y]_+`.

For the rooted residual put `Ebase=[B-y]_+`, `Dbase=D0-y`. Then

`q+E_U >= Ebase+ceil([Dbase-Ebase]_+/2)`,

`f >= (p-lambda)(p+u)+Ebase+ceil([Dbase-Ebase]_+/2)-delta`.

The exact crossing-channel counts are

`C_P=yk+h`, `P_P=yg-h`, `t_P=xy`,

so the exact pair demand is

`2xy <= R_code(S_P)[g+2S_P/L]`.

## Minimal full-support omission design

At `h=0`, every source uses all g matched gamma feet and exactly k of the k+1 U-witnesses. Let `o(s)` be the unique omitted U-witness.

Deletion of `o(s)` from the `(k+1)` U-witness head map gives a bijection onto the k X-heads not occupied by the matched feet. If all sources have one omission type, the omitted vertex is the common buffer and support is not full. If support is full, the omission set has exactly two vertices. Those two U-witnesses share the same X-head; all other U-witness heads are distinct and avoid that shared head. A third omission type is impossible because deleting it would leave the duplicated pair.

This two-omission / duplicated-head model is the next exact object to attack.

## Common buffer retained

For z=1 support of size k, there is one unused buffer. Source visibility gives directly

`H=e(Y)+e(Y,U_d)+e_- <= y`

and any active buffer has `d_X=0`. The previously preserved loaded-buffer strengthening remains:

if `H>0`, `Z>=k(a-1)+x+H`,

with the positive buffer-slack surcharge. The unloaded `H=0` branch is the one-omission minimal model.

## Independent replay / diagnostic

Companion checker in the source-visibility package records:

- 88,200 purified residual instances, zero mismatches;
- 2,083 qualifying finite omission maps for `1<=k<=5`, zero counterexamples to the two-omission theorem.

On the same coarse z=1 parameter box:

- older shared-floor possibilities: 86,820;
- preceding finite-edge-cap union: 77,339;
- full-support possibilities under the source-visibility score floor: 64,275;
- union with the unchanged coarse common-buffer gate: 76,584.

Thus the graph-level source-visibility floor removes a further 755 abstract support-union states relative to the cap-only union. These are diagnostic parameter states, not graph counts.

## Trust boundary / stop-pivot rules

- The source-visibility theorem is graph-level and uses only complete-cut visibility, singleton X-heads of used crossing witnesses, and the independently established same-code localization theorem.
- It does not rely on the invalidated selected-channel claim from z=1 v1.
- The prior cap theorem remains valid but is no longer sharp in full support; do not use its clique equality model as a live survivor.
- Any actual graph/formula mismatch remains an immediate blocker.
- Do not treat finite parameter counts as D2C graph counts.
- Exhaust the two-omission duplicated-head model and the unloaded common-buffer model before beginning z=2.
- Keep `X_3` hostile, the mixed `{4,5}` ladder closed, the four-exception gate subordinate, and first-proof priority on Erdos #742 inactive.
<!-- CURRENT-STATUS:END -->
