# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_K2_Y_INDEPENDENCE_LOW_A_SLACK_2026_09_20`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `49cf219d759dfeebe447f7af7cf8f86dab836272`; before forward mathematics this invocation reread CURRENT_STATE.md, README.md, the latest commits, the 20 September daily red-team audit, the global H/Y polarization, B0 source-collapse, weighted source-collapse optimization, residual-one/private-coordinate profile, residual-hub theorem, and the exact A-side compensator identity. The audit gate remained binding throughout.

LAST VERIFIED RESULT: `Same-session conditional strengthening plus hostile scope correction. In residual one with k=2 and J2=empty, G[Y] is exactly independent: any Y--Y edge is same-code d and requires a complementary U_bar(d) witness; B0 witnesses are Y-anticomplete, while every U_bar(d) witness outside B0 has an H-neighbour which is automatically an illicit second common neighbour because Y is complete to H. Hence e(Y)=0 and the predecessor dense-Y compensator arm is removed. Every H--H edge has an exact certificate dichotomy: private foot q_i/q_l, or an endpoint-indexed Y-anticomplete U witness in U_{bar(d) xor e_l}/U_{bar(d) xor e_i}. Summing disjoint endpoint-indexed code classes shows that at most u=p+1 H--H edges can be U-certified. If S is the set of H-heads touched by B+=U_bar(d)\B0, private-foot orientations fail on H[S], so e(H[S])<=p+1 and e(H)<=binom(p-1,2)-binom(|S|,2)+(p+1). B+ also pays E_U(B+)>=|B+|[p-3-|S|]_+. Consequently all low-A-slack compensation is forced into a dense, mostly private-foot-certified H layer. Quantitatively, if ell=L_A/p^2, then |B0|/p<=ell+o(1), the H-positive escape density is <=ell+o(1), e(U)/p^2<=ell+o(1), M_U/p^2>=1/2-ell-o(1), e(H)/p^2>=(1-2ell)/2-o(1), and e(Y,U)/p^2>=1-ell-o(1). A tempting extension of the earlier F0--Y reverse-certificate argument to every H-free Y--U edge was rejected: the convenient two-orientation lemma is triangle-edge scoped. For a triangle-free Y--U edge ty one instead has N_X(t)=empty, N_U(t) cap N_U(y)=empty, and the exact endpoint slack bill epsilon_t+epsilon_y>=p.`

UNPRESERVED WORK: `None after atomic preservation. New notes: ONE_CODE_R1_K2_Y_INDEPENDENCE_AND_HH_CERTIFICATE_SPLIT.md and ONE_CODE_R1_K2_LOW_A_SLACK_NORMAL_FORM_AND_TRIANGLE_SCOPE.md. No finite scan is used as proof.`

DEFERRED ADMIN: `README remains deliberately at the independently audited public checkpoint. Do not promote the same-session Y-independence, H--H private-foot certificate split, B+ touched-set sparsity, low-A-slack product normal form, or triangle-free endpoint-slack lemma before the next daily hostile audit. Historical notes remain preserved. The prior q=1 golden forward/reverse/dual witness split and W_min curve remain superseded by ONE_CODE_R1_K2_F0_Y_FORWARD_BRANCH_CORRECTION.md.`

NEXT ACTION: `First independently hostile-replay Y-independence, especially the use of the repaired same-code witness theorem and the B0/B+ split. Then replay the H--H private-foot/U-witness dichotomy and the claim that total U-certified H--H edges is at most p+1. If they survive, attack the new low-A-slack hostile normal form: H dense and mostly private-foot-certified, Y independent, H--U sparse, Y--U dense, U sparse. Partition Y--U edges explicitly into triangular and triangle-free parts: use reverse-witness capacity only on the triangular part and the exact epsilon_t+epsilon_y>=p bill on the triangle-free part. Do NOT extend the triangle-only two-orientation lemma to arbitrary H-free Y--U edges. In parallel, use private-foot contamination to constrain the private-coordinate support of Y-dense H-free U vertices. Keep y=o(p) separate under g0=p-y and the large-gap/rooted residual machinery.`

AUDIT GATE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed in later repair work; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans are diagnostics only; superseded evidence stays superseded; bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3.`

SECOND STRICT: `Provisional exact unloaded second-strict chain remains finite-order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. Pending hostile replay.`

R1 K2 LIVE CHAIN: `Residual-one common hub/beta fan -> exact low-k k=2 ray -> escape-class budgets -> cross-code complement localization -> compressed half barrier -> hostile correction of F0--Y forward witness -> global escape H/Y polarization -> exact A-hole floor -> hole/slack coupling -> U-density source localization -> B0 H/Y-antichain and independence -> quarter missing-U floor -> weighted source-collapse minimum 11-4sqrt(5) -> exact Y-independence -> H--H private-foot/U certificate dichotomy -> at most p+1 U-certified H--H edges -> B+ touched H-set sparse -> low-A-slack product normal form -> triangular versus triangle-free Y--U attack; y=o(p) remains a separate large-gap route.`

R1 K2 SUPERSESSION: `The predecessor claim that dense Y compensation can be supplied by B+=U_bar(d)\B0 is false. Although raw same-code localization places a putative Y--Y witness in U_bar(d), B0 cannot witness because it is Y-anticomplete and B+ cannot witness because any H-neighbour is an extra common neighbour with the Y source. Thus e(Y)=0 exactly. Separately, do not infer reverse singleton certification for an arbitrary H-free Y--U edge: the convenient two-orientation lemma is triangle-edge scoped; triangle-free Y--U edges are handled by the endpoint-slack theorem instead.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:
- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in later repair;
- `X_3` mandatory negative control;
- zero positive actual D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until hostile replay.

## Live structural chain

`audited rooted/Hall setup`
`-> rigid singleton-head implication`
`-> provisional unloaded second-strict finite-order closure`
`-> one-code residual-one common hub / beta fan`
`-> k=2 low-k/high-y escape`
`-> global escape H/Y polarization`
`-> B0 H/Y-antichain and source collapse`
`-> exact A-side compensator identity`
`-> Y--Y compensation eliminated: e(Y)=0`
`-> H--H certificate split: private foot or endpoint-indexed U witness`
`-> only O(p) U-certified H--H edges`
`-> B+ touched H-set internally sparse`
`-> low A-slack forces H dense, H--U sparse, Y--U dense, U sparse`
`-> triangular / triangle-free Y--U partition is now load-bearing`.

## Session ledger — current invocation

**Invocation start:** 21:01:08 BST.  
**Planned preservation cutoff:** 21:55:38 BST.

Substantive units completed:
1. reconciled CURRENT_STATE.md, README.md, live head `49cf219...`, latest commits, and the 20 September daily red-team audit before forward mathematics;
2. independently replayed the global escape H/Y polarization and found no new flaw at its stated triangle-edge scope;
3. replayed the B0 Y-antichain, B0 independence, and oriented U-source collapse, preserving their conditional scope;
4. audited the supposed dense-Y compensator arm and proved the stronger exact theorem `e(Y)=0`; this explicitly corrects the predecessor claim that B+ can act as a usable Y--Y witness;
5. derived the exact H--H certificate dichotomy: private foot or endpoint-indexed Y-anticomplete U witness;
6. summed the endpoint-indexed witness classes to show that at most `u=p+1` H--H edges can be U-certified globally;
7. proved that the B+-touched H-set S has `e(H[S])<=p+1`, hence a large B+ support destroys dense-H compensation;
8. derived the complementary B+ slack bill `E_U(B+)>=|B+|[p-3-|S|]_+`;
9. synthesized the quantitative low-A-slack normal form: H dense, Y independent, H--U sparse, Y--U dense, U sparse, and B0 small;
10. hostile-tested a tempting extension of the F0 reverse-certificate capacity to H-free Y--U edges and rejected it because the raw two-orientation lemma is not valid off triangle edges;
11. replaced that invalid shortcut by the exact triangle-free edge theorem `epsilon_t+epsilon_y>=p`, preserving the correct next triangular/triangle-free partition.

**Live unfinished line:** hostile-replay Y-independence and the H--H private-foot split, then attack the low-A-slack product normal form by separating triangular Y--U edges (certificate capacity) from triangle-free Y--U edges (endpoint slack), while using dense-H private-foot certificates to constrain U private-coordinate supports.

**Stop reason:** final preservation checkpoint after eleven tightly connected mathematical/audit units. No mathematical blocker; the next partitioned Y--U / private-foot attack is preserved precisely for the following invocation.