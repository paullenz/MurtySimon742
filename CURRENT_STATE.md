# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is **not** the target. The published 2024 D2C hostile control `X_3` has 12 vertices and 32 edges while `M(12)=31`; it remains mandatory. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not an optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ALL_R_TYPED_CROSS_HOLE_LEDGER_2026_09_20`

WORK MODE: `MATH_AFTER_AUDIT_GATE`

INSPECTED PREDECESSOR: `c3a82e10a89406770d71ed613671296ee7f0f302`

LAST VERIFIED RESULT: `The 20 September daily red-team audit remains binding. Its mandatory upstream same-code criticality / ordered (source,witness) audit has now been independently passed at raw D2C level by c3a82e10..., including all witness locations and both orientations. That audit also proves the stronger internal-X head-reserve r_w<=x-2 and witness slack epsilon_w>=[p-x+r_w+2]_+. Building only on that repaired interface, the current run derives: (i) the injective core-head map into X\{a_0} forces g=x-k>=1; (ii) if m physical z-nonneighbours are actually used for internal-X certificates then epsilon_{a_0}>=p+u-y-d+m-1 and the combined z/a_0 floor rises by m; (iii) those witnesses force located X-U and A-U holes, giving Z_X>=Z_X^0+NJ+e+m and Z>=Z_0+NJ+e+m(y+1); (iv) the exact fixed-m internal load price is P_int(e,m)=[e-m(x-p-2)]_+, producing e>=B_d+m+P_int; (v) for y>=2 every X-Y edge is U-certified either by a bar-C witness with one graph-fixed Y-head or a bar-d witness with one graph-fixed X-head; after the k core vertices are credited their maximal ky edges, at least R=gy cross edges require outside witnesses among the d z-nonneighbours; (vi) internal-X and outside cross witnesses are physically disjoint, so m+s<=d, every used cross witness also misses a_0, and cross load self-prices; (vii) type-aware cross certificates force Z_X>=Z_X^0+NJ+e+m+R_C+s_D(x-1) and Z>=Z_0+NJ+e+m(y+1)+R+s_C(y-1)+s_D(x-1). A focused typed replay leaves 62,800 abstract rows admitting an e(X)=0 tuple (58,193 with y>=2; 4,607 with y=1). This is a slice diagnostic only, not a total survivor count or graph count.`

UNPRESERVED WORK: `None from this run. New hand packages are project/research/post_ms/2026-09-20-all-r-exact-local-optimization-v1/ALL_R_EXACT_LOCAL_OPTIMIZATION.md, CROSS_EDGE_RESERVOIR_SHARPENING.md, and TYPED_CROSS_HOLE_LEDGER.md. Companion diagnostics check_all_r_exact_local_optimization.py and check_all_r_cross_reservoir.py are committed. The latter records 71,996 rows for the type-relaxed cross-witness system. The stronger typed Z_X/Z ledger has so far been replayed only on the e(X)=0 slice; no total typed survivor count is promoted.`

DEFERRED ADMIN: `README remains synchronized to the 20 September daily audit checkpoint. The public-facing status should not yet be advanced to these same-hour conditional sharpenings before independent replay / the next adversarial checkpoint. Minor inherited checker hygiene remains: the dead t-choice output in check_all_r_pinch_local_feedback.py should be cleaned before reviewer-facing release.`

NEXT ACTION: `Stay inside the literal all-R equality pinch. First independently replay the newly committed exact-local and cross-reservoir diagnostics and then implement a total typed (R_C,R_D,s_C,s_D,e,m,d) checker using ZX-TYPED and Z-TYPED; do not promote a total count until that replay is stable. Structurally, the dominant live arm is e(X)=0, where A=K_{x,y}. The obvious attempt to reapply raw criticality to a cross witness--head edge is a dead end because the original source recycles as its certificate; preserve this obstruction. The next hand attack should instead exploit simultaneous multi-source load on a fixed-head witness or interactions between two distinct fixed-head witnesses, seeking genuinely new physical U-U or A-U incidences. Keep the y=1 slice separate because the special matched foot can certify X->Y edges there. In parallel after this local step, the audit-required zero-positive-fixture rigid-cut coverage gap remains high priority: construct a positive actual D2C rigid complete-cut fixture or prove the rigid implication/nonrealizability directly. Keep exact pair-local Ccap_P, ONE-P and CROWD local. Do not open m>=2, loaded-buffer, z=2 or the four-exception route until this audited arm closes or proves insufficient.`
<!-- CURRENT-STATUS:END -->

---

## 1. Binding 20 September adversarial-audit gate

The latest daily audit is:

`project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Its verified/reproduced trust boundary remains binding:

- raw P1 distinct physical beta-source identity is proved;
- P2 is selected-representative uniqueness for one physical `(source,coordinate)` obligation, not raw-witness uniqueness;
- the corrected source-premise atlas replay gives 21 D2C classes through order seven, 126 roots, 2 raw beta certificates, 1 raw alpha certificate, and zero P1/raw-P2 collisions;
- `X_3` independently replays as D2C with `n=12`, `m=32>M(12)=31`;
- exact-core first-strict arithmetic independently replays as `248,798 / 208,684 / 174,769` for coarse / pair / final rows;
- the audited all-R diagnostic independently replays as `173,347` predecessor final rows and `124,528` local-feedback rows;
- the piecewise predecessor `e_req(d)` formula was independently brute-force checked.

Superseded evidence must remain superseded:

- the pre-repair alpha-labelled-as-beta checker result;
- the invalid matched-only common-buffer orientation theorem;
- 188,912 first-strict rows lacking physical reservoir capacity;
- 179,735 intermediate rows before exact-core restoration;
- 178,572 weak-core one-witness rows;
- any abstract survivor count interpreted as a graph count or realizability statement.

High-risk interfaces retained from the audit:

1. no bounded actual-D2C fixture realizes a rigid complete Hall cut with `x>=3`;
2. same-code raw criticality / ordered pair injection was load-bearing and required fresh proof;
3. exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` cannot be replaced by total-score slack.

Item 2 has now been independently re-derived in:

`project/research/post_ms/2026-09-20-same-code-raw-criticality-audit-v1/SAME_CODE_RAW_CRITICALITY_AUDIT.md`.

Items 1 and 3 remain open/binding.

---

## 2. Raw same-code audit passed

Commit `c3a82e10a89406770d71ed613671296ee7f0f302` re-derives from raw D2C edge criticality:

- every same-code edge in `G[A union U]` has a selected orientation/source and complementary-code witness in `A union U`;
- a U-source requires an A-witness;
- one fixed ordered `(source,witness)` pair certifies at most one selected same-code edge;
- each certificate pays `epsilon_source+epsilon_witness>=lambda+1`.

In the literal all-R pinch it additionally proves:

- a used internal-X witness can certify at most `x-2` internal edges (head reserve);
- a used internal-X witness of load `r` pays `epsilon_w>=[p-x+r+2]_+`.

This satisfies the daily audit's mandatory upstream proof gate. It does **not** resolve the separate rigid-cut realizability gap.

---

## 3. Current literal all-R geometry

The live conditional branch has:

- `A=X dotcup Y`, `X--Y` complete, `|X|=x>=3`, `|Y|=y>0`;
- all X code `C`, all Y code `d`, `d_H(C,d)=1`;
- `a_0` is isolated in `G[X]`;
- `U_-=W_0 dotcup {b}`, `|W_0|=k`, and the core head map is injective into `X\{a_0}`;
- `g=x-k>=1`;
- `z in U_o` has code `bar C`, `N_A(z)={a_0}`, and misses all core vertices;
- `d` counts z-nonneighbours in `U_o\{z}`; `J=u_o-1-d` z-neighbours are `bar C`, A-anticomplete, and independent;
- internal X-edge witnesses can occur only among the d z-nonneighbours.

Let m be the number of actually used physical internal-X witnesses, with total selected load `e=e(X)`. Then

`m<=d`,

`m<=e<=m(x-2)` for `m>0`,

`P_int(e,m)=[e-m(x-p-2)]_+`,

`epsilon_{a_0}>=p+u-y-d+m-1`,

`Z_X>=Z_X^0+NJ+e+m`,

`Z>=Z_0+NJ+e+m(y+1)`,

and exact Hall forces

`e>=B_d+m+P_int(e,m)`.

These are physical-witness refinements of the predecessor `d`-only relaxation.

---

## 4. Complete-cut raw criticality and cross reservoir

For `y>=2`, every edge of the complete `X--Y` cut has only two U-witness orientations after root/A/matched locations are excluded:

- X-source -> witness code `bar C`, with graph-fixed single Y-head;
- Y-source -> witness code `bar d`, with graph-fixed single X-head.

The k core vertices can cover at most `ky` such edges. Buffer b, distinguished z, and all J purified z-neighbours cover none. Therefore at least

`R=xy-ky=gy`

cross edges require outside witnesses among the d z-nonneighbours.

If `s_C,s_D` are the used outside populations and `R_C,R_D` their selected loads, then

`R_C+R_D=R`,

`s_C<=R_C<=s_C x`,

`s_D<=R_D<=s_D y`,

`m+s_C+s_D<=d`.

The type-aware slack floor is

`P_cross >= [R_C-s_C[x-p+1]_+]_+ + R_D+s_D(p-y-1)`.

Every such used cross witness misses `a_0`, so

`epsilon_{a_0}>=p+u-y-d+m+s_C+s_D-1`.

Physical hole ledgers sharpen to

`Z_X>=Z_X^0+NJ+e+m+R_C+s_D(x-1)`,

`Z>=Z_0+NJ+e+m(y+1)+R+s_C(y-1)+s_D(x-1)`.

These are the live quantities to feed to exact Hall, U-score, q and residual identities.

For `y=1`, do not use this cross-reservoir theorem: the special matched endpoint may in principle certify X-source cut edges.

---

## 5. Diagnostics and evidence labels

Daily-audited predecessor all-R local-feedback count:

- `173,347` predecessor equality-pinch rows;
- `124,528` after the audited d-based local-feedback package.

New same-hour diagnostics:

- exact `(d,m,e)` used-witness system plus crude cross capacity: `78,582` rows;
- type-relaxed cross witness score/population system: `71,996` rows;
- focused **typed `e(X)=0` slice** under `ZX-TYPED/Z-TYPED`: `62,800` rows admitting at least one `e=0` tuple, of which `58,193` have `y>=2` and `4,607` have `y=1`.

Only the first two new total counts have committed companion checkers. The 62,800 value is a focused slice calculation, not a total count. All three are provisional same-hour diagnostics pending independent replay/adversarial audit.

The attempted next idea of reapplying raw criticality to a cross witness--head edge fails to create a new obligation: the original source itself certifies that edge in the reverse role. This recycled-certificate obstruction is preserved in `TYPED_CROSS_HOLE_LEDGER.md` and should prevent double-counting in later work.

---

## 6. Session ledger — 20 September early-hours run

**Invocation start:** approximately 01:29 BST.  
**Planned preservation cutoff:** approximately 01:55 BST, before the next hourly invocation.  

Substantive units completed in this invocation:

1. reconciled the new raw same-code audit with the binding daily red-team gate;
2. proved `g>=1` from the injective core-head map into `X\{a_0}`;
3. derived the m-sensitive `a_0` degree / z-a0 conservation law;
4. derived physical internal-witness `Z_X` and `Z` hole ledgers;
5. derived the exact fixed-m internal witness load price and Hall inequality;
6. classified raw X--Y cut certificates for `y>=2` into fixed-head `bar C` / `bar d` witness stars;
7. derived the outside cross-witness population and slack tradeoff, disjointness from internal witnesses, and cross source-hole ledger;
8. refined this to typed `R_C/R_D, s_C/s_D` physical `Z_X/Z` bounds;
9. ran a focused typed `e(X)=0` diagnostic (62,800 slice rows) and preserved the result with its evidence label;
10. tested the natural witness-head criticality follow-on, found that it recycles the original source certificate, preserved that obstruction, and pivoted the next target to multi-source / two-witness interactions.

**Live unfinished line:** derive a genuinely new incidence from simultaneous load on one fixed-head cross witness or interactions between distinct fixed-head witnesses; separately build a total typed checker rather than extrapolating from the `e=0` slice.

**Why this run stopped:** preservation cutoff approached; no mathematical blocker. New exploration was stopped in favor of durable handoff and avoiding overlap with the next invocation.

---

## 7. Mandatory next-hours programme

1. Independently replay both new committed diagnostics; discrepancies are blockers and must be preserved.
2. Build the total typed checker retaining `(d,m,e,s_C,s_D,R_C,R_D)` and `ZX-TYPED/Z-TYPED`; do not infer a total from the `e=0` slice.
3. On the structural side, attack multi-source fixed-head witnesses and pairwise interaction of distinct cross witness stars. Count only genuinely new physical incidences; the witness-head edge itself can recycle the source and gives no free second certificate.
4. Keep `y=1` separate and inspect the special matched-foot geometry directly.
5. Resolve the audit's zero-positive-fixture rigid-cut gap in parallel once this local equality arm reaches a natural checkpoint: positive actual fixture, publication-grade direct rigid implication, or proof of nonrealizability.
6. Feed only physical U-holes/slack and exact Hall quantities into `Q=e(G[U])`, `E_U`, `r`, `f`, and `delta=r-e(F)`.
7. Do not infer an asymptotic threshold from the bounded boxes and do not open larger reservoirs / loaded buffers / `z=2` / four-exception work merely because a diagnostic shrinks.