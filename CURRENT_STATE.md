# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_K2_HEAVY_ENDPOINT_2026_09_20`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `5cfeaf5ad002bf6434ed936d91f98885936db7b9`; before forward mathematics this invocation reread CURRENT_STATE.md, README.md, the latest commits, the 20 September daily red-team audit, the residual-one orientation-covering and physical-reprice notes, the exact pair-local scalar-gate definitions, and the residual-hub raw B-edge criticality theorem.

LAST VERIFIED RESULT: `Same-session hostile replay found no overlap/location defect in OC-RECT through OC-RES. Full optimization exposed an exact unbounded low-k/high-y family c=p=lambda=t, y=t-1, k=2, u=x=t+1, n=5t+2 which survives strengthened score/rooted and aggregate exact Ccap_P/(ONE-P)/(CROWD). At k=2 every mixed escape touching both K and W_s pays epsilon_w>=p+1, and exact degree conservation is sharp under a one-sided K-heavy/W-heavy partition. Raw B-edge criticality now further collapses the W-heavy endpoint: if w is anticomplete to K but adjacent a used witness z_h, reverse orientation is impossible and the edge w z_h must orient forward through h, giving N(w) cap N(h)={z_h}. Hence every W-heavy escape has N_Y(w)=empty, misses q_j, and is anticomplete to every K-heavy escape. Thus r_W W-heavy vertices add y r_W located Y--U nonedges and the two heavy sides lose r_K r_W U--U pairs.`

UNPRESERVED WORK: `None: ONE_CODE_R1_LOW_K_ESCAPE_AND_MIXING_DICHOTOMY.md, ONE_CODE_R1_K2_ONESIDED_CONSERVATION.md, ONE_CODE_R1_K2_HEAVY_ENDPOINT_CRITICALITY.md, check_r1_low_k_escape.py, and this exact handoff are committed. The checker is diagnostic only.`

DEFERRED ADMIN: `README remains deliberately at the independently audited public checkpoint. Do not promote same-session low-k residual-one claims before the next daily hostile audit. Historical failed/superseded claims and CI failures remain preserved.`

NEXT ACTION: `Hostile-replay the heavy-endpoint orientation first. If it survives, the most permissive remaining asymptotic endpoint is predominantly K-heavy with J2 small, ultimately J2=0: escapes adjacent both same-code C-heads a,b and anticomplete to W_s, with an all-radius-one matched layer. Attack that literal geometry by raw criticality of K-heavy incident edges/spokes and by the rooted Q/residual ledger. Retain the W-heavy Y--U holes and K-heavy/W-heavy missing U block if both types coexist. Do not collapse them prematurely into total slack.`

AUDIT GATE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed in later repair work; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans are diagnostics only; bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3.`

SECOND STRICT: `Provisional exact unloaded second-strict chain remains finite-order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. Pending hostile replay.`

R1 PREDECESSOR: `At r=p-m=c-d_U=1, K has code C=d xor e_j, matched heads h_i have support {i} or {i,j}, A_bar(C)=empty for p>=3, |U_bar(C)|<=c-1, and the old high-k y=1 exact stress ray is finite after orientation covering.`

R1 HOSTILE REPLAY: `The q_j hub beta orientation, W_s--Y=empty, W_s--U_bar(d)=empty and K--H=empty have been independently replayed in the present structural chain. All remain conditional on rigid-interface reachability.`

R1 PHYSICAL REPRICE: `For f:K->K, E=U\W_s and beta-forced W_s--E holes P, E_Ws>=k(p+k-2)+P and L_X+P>=T(s_f)=k(g0+2)+s_f(c-2), alongside the exact Hamming floor L0(s_f).`

R1 ORIENTATION COVER: `Same-session hostile replay confirms (A_w cap B_w) x (S_w cap T_w)=empty with correct degree-universe/disjointness accounting, hence epsilon_w>=min(k,p-1).`

R1 LOW-K ESCAPE: `For every t>=4, c=p=lambda=t, y=t-1, g0=1, k=2, u=x=t+1, m=t-1, s_f=2 gives n=5t+2. L0=floor(5t/2)+2, T=2t+2, OC score floor=floor(13t/2), C0=floor((3t^2+10t-4)/2); exact rooted and pair-local aggregate gates remain open. This is a method escape family, not a graph construction.`

R1 K2 MIXING: `The beta transposition forces two cross holes for every escape. A mixed escape forces the J1 opposite rectangle empty, cannot be J2-bad, and therefore pays epsilon_w>=p+1. Cheaper escapes are one-sided.`

R1 K2 CONSERVATION: `With A=e(K,E), B=e(W_s,E), exact degree formulas give E_K=2(lambda+1)-A and E_Ws=2(p+c-1)-B; beta cross holes give A+B<=2(c-1), hence E_K+E_Ws>=2(p+lambda+1). Mixed and J2-serving refinements are preserved in ONE_CODE_R1_K2_ONESIDED_CONSERVATION.md. The current incidence lower bound is sharp via saturated K-heavy/W-heavy types, so aggregation alone is insufficient.`

R1 K2 HEAVY ENDPOINT: `If w is anticomplete to K but w z_h is an edge, rooted B-edge criticality cannot orient from z_h: a Y witness creates the extra common head h, an H witness creates the private matched common neighbour q_i, and a K witness is unavailable. Thus orientation is forced from w through witness h and N(w) cap N(h)={z_h}. A W-heavy w therefore has N_Y(w)=empty and wq_j absent. Every W-heavy escape is anticomplete to every K-heavy escape. Hence Z_Y gains y r_W beyond the selected-witness holes and the heavy partition loses r_K r_W U--U pairs. The remaining permissive endpoint is K-heavy with J2 near zero.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:

- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in the subsequent upstream repair;
- `X_3` mandatory negative control;
- zero positive actual-D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until independent adversarial replay.

## Live structural chain

`audited rooted/Hall setup`
`-> rigid singleton-head implication`
`-> provisional unloaded second-strict finite-order closure`
`-> one-code private-coordinate exhaustion r=c-d_U`
`-> residual-one common hub / beta fan`
`-> physical repricing and private F/R matrix`
`-> hostile-replayed universal escape floor`
`-> old high-k ray finite`
`-> exact k=2 high-y escape ray`
`-> beta cross-hole and mixed-escape penalty`
`-> exact one-sided K/W conservation`
`-> saturated K-heavy/W-heavy endpoint`
`-> W-heavy forced B-edge orientation`
`-> W-heavy Y-anticompleteness and K-heavy/W-heavy anticompleteness`
`-> live predominantly-K-heavy, J2-small attack`.

## Session ledger — current invocation

**Invocation start:** 16:30:32 BST.  
**Planned preservation cutoff:** 16:55:38 BST.

Substantive units completed:

1. reconciled CURRENT_STATE, README, latest commits and the 20 September daily red-team audit before forward mathematics;
2. independently hostile-replayed `OC-RECT` through `OC-RES`, including exact degree-universe and disjointness checks;
3. optimized the strengthened residual-one gates and extracted the exact unbounded k=2/high-y family;
4. verified that family analytically against exact score and rooted-Q gates;
5. replayed exact pair-local `Ccap_P`, purified `(ONE-P)` and scalar `(CROWD)` and proved the aggregate gates remain open;
6. derived the k=2 beta cross-hole normal form;
7. proved mixed escapes pay `epsilon_w>=p+1`;
8. derived exact K/W incidence-slack conservation;
9. derived mixed-count/J2 refinements and characterized the saturated one-sided K-heavy/W-heavy endpoint, proving degree aggregation alone is a method boundary;
10. attacked a W-heavy escape by raw B-edge criticality and proved its incident used-witness edges have forced forward orientation through the corresponding K-head;
11. deduced W-heavy Y-anticompleteness, the residual-hub nonedge, complete K-heavy/W-heavy anticompleteness, and the resulting located `y r_W` Y--U and `r_K r_W` U--U defect blocks;
12. added the diagnostic checker and preserved all theorem/diagnostic/handoff artifacts.

**Live unfinished line:** hostile-replay the new W-heavy forced-orientation proof, then attack the predominantly K-heavy / J2=0 endpoint. Preserve matched-coordinate and rooted-Q location information; do not return to undifferentiated slack aggregation.

**Stop reason:** preservation checkpoint after twelve tightly connected mathematical/audit units. No mathematical blocker; the next coherent structural line is preserved for immediate continuation.
