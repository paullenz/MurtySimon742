# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_K2_Q1_WEIGHTED_CERTIFICATE_LEDGER_2026_09_20`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `2a84aaf86a42ca9d96fc093121f373140d40116d`; this invocation began by rereading CURRENT_STATE.md, README.md, the latest commits, and the 20 September daily red-team audit before forward mathematics. The audit gate remains binding. The run independently replayed the additive half-barrier minimization, attacked the pure-F0 equality normal form by raw F0--Y edge criticality, retained forward/reverse witness overlap, and finally corrected the comparison into the exact weighted rooted/score ledger.

LAST VERIFIED RESULT: `Same-session conditional strengthening. The additive functional satisfies F>=1/2+(1-delta)^2/2. In the pure-F0 endpoint, raw D2C criticality of a typical F0--Y edge forces forward witnesses to be atypical and Y-sparse, while reverse witnesses are atypical and X-anticomplete. Fixed ordered certificate pairs certify at most one endpoint. If y/p->q>0, certificate capacity plus located-hole pricing gives (Z_Y+Z_X)/p^2>=min(q^2,tau)-o(1), where tau=|T|/p; hence coefficient-1/2 high-Y pure-F0 equality is impossible. On the exact q=1 ray, splitting exceptional witnesses into forward-only F, reverse-only R and dual-role B gives tau(f+b)+(r+b)>=tau and f+r+b<=1-tau, hence tau<=rho=(sqrt(5)-1)/2=0.618033... . Golden equality forces all exceptional witnesses dual-role at density 1-rho=(3-sqrt(5))/2; such witnesses are simultaneously Y-sparse and X-anticomplete. The exact U-side identity then gives epsilon_w>=p-o(p) on every dual witness. Forward certificates also consume distinct T--witness U-nonedges. In the correct combined weighted ledger E_U+Z_X+Z_Y+2M_U+2L_A, the certificate subsystem has exact leading lower envelope W_min(tau)=tau^2+tau for tau<=1/2 and W_min(tau)=tau^2+3tau+3-2/tau for 1/2<=tau<=rho; at tau=rho, W_min=2. This is a substantial tightening but not closure because the legitimate combined ceiling has leading coefficient 4. A same-session phrase comparing unweighted D_phys coefficient 3/2 directly with C0 was invalid and is explicitly superseded in ONE_CODE_R1_K2_LEDGER_SCOPE_CORRECTION.md.`

UNPRESERVED WORK: `None after atomic preservation. New/updated notes in project/research/post_ms/2026-09-20-rigid-interface-direct-audit-v1/: ONE_CODE_R1_K2_PURE_F0_Y_EDGE_CERTIFICATE_OBSTRUCTION.md, ONE_CODE_R1_K2_Q1_CERTIFICATE_WITNESS_SPLIT.md, and ONE_CODE_R1_K2_LEDGER_SCOPE_CORRECTION.md. No finite scan was used.`

DEFERRED ADMIN: `README remains deliberately at the independently audited public checkpoint. Do not promote the same-session half-barrier, F0--Y certificate obstruction, golden witness split, or weighted ledger refinement before the next daily hostile audit. Historical invalidated/superseded claims remain preserved.`

NEXT ACTION: `First hostile-replay the F0--Y witness exclusions, the golden witness-split LP, and the dual-witness slack estimate against the exact rooted partition. If they survive, attack the dual-role witness class directly: classify its U-neighbourhood under simultaneous X-antichain/Y-sparsity and the requirement to serve both certificate orientations, seeking forced additional U-nonedges or A-holes that consume the remaining weighted-ledger budget. For tau<rho, combine the exact W_min(tau) curve with the predecessor hub/support departure costs. Separately follow the forced y=o(p) redirection through g0=p-y and the existing large-gap/rooted residual machinery.`

AUDIT GATE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed in later repair work; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans are diagnostics only; superseded evidence stays superseded; bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3.`

SECOND STRICT: `Provisional exact unloaded second-strict chain remains finite-order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. Pending hostile replay.`

R1 K2 LIVE CHAIN: `Residual-one common hub/beta fan -> exact low-k k=2 ray -> one-sided escape partition -> D1/K-heavy hub-private compression -> raw cross-code complement localization -> proper-support nonhub independence -> complement-witness capacity -> F0 hub/private compression -> compressed P normal form -> global half barrier -> additive/stability refinement -> pure-F0 equality normal form -> F0--Y certificate-capacity/located-hole obstruction -> q=1 golden witness split -> exact weighted certificate envelope -> live dual-witness U-neighbourhood attack / low-y redirection.`

R1 K2 PURE F0: `Coefficient-1/2 pure-F0 high-Y equality is eliminated. Forward F0--Y certificates force Y-sparse atypical witnesses; reverse certificates force X-anticomplete atypical witnesses. On q=1, no certificate assignment exists with typical-F0 density tau>rho=(sqrt(5)-1)/2. At the extremal feasible tau, all exceptional witnesses are dual-role and simultaneously Y-sparse/X-anticomplete, with epsilon_w>=p-o(p). The correct weighted certificate contribution is W_min(rho)=2 against a combined leading ceiling 4; no closure is claimed.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:
- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in later repair;
- `X_3` mandatory negative control;
- zero positive actual D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until hostile replay.

## Live structural chain

`audited rooted/Hall setup`
`-> rigid singleton-head implication`
`-> provisional unloaded second-strict finite-order closure`
`-> one-code residual-one common hub / beta fan`
`-> k=2 low-k/high-y escape`
`-> escape-class physical budgets`
`-> hub/private-spoke compression`
`-> cross-code complement localization/capacity`
`-> compressed proper-support P normal form`
`-> compressed global half barrier`
`-> additive stability refinement`
`-> pure-F0 equality endpoint`
`-> F0--Y raw certificate-capacity / located-hole obstruction`
`-> q=1 golden witness split`
`-> exact weighted certificate envelope`
`-> dual-witness U-neighbourhood attack / low-y redirection`.

## Session ledger — current invocation

**Invocation start:** 19:28:22 BST.  
**Planned preservation cutoff:** 19:55:38 BST.

Substantive units completed:
1. reconciled CURRENT_STATE.md, README.md, live commits at `2a84aaf...`, and the 20 September daily red-team audit before forward mathematics;
2. independently replayed the additive half-barrier and derived `F>=1/2+(1-delta)^2/2`, plus quantitative stability;
3. re-derived the generic two-orientation edge-criticality lemma directly from diameter-2-criticality;
4. classified forward criticality of a typical pure-F0--Y edge and localized forward witnesses to the atypical escape reservoir;
5. classified reverse criticality and proved every reverse witness must be X-anticomplete;
6. derived the certificate-capacity threshold `q(1-e)<=e(1-e+q)` and `e_*(q)=[1+2q-sqrt(1+4q^2)]/2`;
7. sharpened the classification into physical pricing: forward witnesses are Y-sparse, reverse witnesses X-anticomplete, giving `(Z_X+Z_Y)/p^2>=min(q^2,tau)-o(1)`;
8. specialized to the exact q=1 ray and eliminated coefficient-1/2 high-Y pure-F0 equality;
9. retained overlap between forward/reverse witness populations and solved the exact asymptotic certificate LP on q=1;
10. proved `tau<=rho=(sqrt(5)-1)/2`, with golden equality forcing a dual-role exceptional reservoir of density `(3-sqrt(5))/2`;
11. derived dual-witness U-slack `epsilon_w>=p-o(p)` from the exact U-side degree identity;
12. observed that every forward certificate consumes a distinct T--witness U-nonedge and priced that block in `M_U`;
13. minimized the certificate subsystem in the correct weighted ledger, obtaining `W_min(tau)=tau^2+tau` for `tau<=1/2`, `W_min=tau^2+3tau+3-2/tau` for `1/2<=tau<=rho`, and `W_min(rho)=2`;
14. audited ledger scope against the preserved weighted-rooted theorem and caught/superseded the same-session invalid comparison of unweighted `D_phys` coefficient `3/2` directly with score ceiling `C0`;
15. preserved the theorem notes, correction note, and synchronized this handoff without promoting same-session claims to README.

**Live unfinished line:** hostile-replay dual-witness slack and certificate LP, then attack the dual witnesses' U-neighbourhood and combine the exact W_min curve with support/hub departure costs; separately test the forced `y=o(p)` regime.

**Stop reason:** preservation phase after fifteen tightly connected mathematical/audit units. No mathematical blocker; the next raw dual-witness attack is preserved precisely.