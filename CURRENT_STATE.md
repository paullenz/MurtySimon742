# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_RESIDUAL_HUB_CRITICALITY_2026_09_20`

WORK MODE: `MATH_AFTER_AUDIT_GATE`

LATEST_RECONCILED_PREDECESSOR_HEAD: `aa9f246c2df30d3765b361c6ff19163fa9b1252d`.

LATEST_THIS_RUN_COMMITS: `04f5e2bd234407babb522c5a72679c1a906f7cec` (residual-one hub criticality theorem package), `bb0ceb44ba2af1e416f9570d8e7178126da88e51` (escape-ray arithmetic replay), plus this state synchronization commit.

LAST VERIFIED/AUDITED BASE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed in the subsequent repair work; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans remain diagnostics only; and bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3. The zero-positive-fixture interface remains the dominant global risk.`

SECOND_STRICT_STATUS: `Same-session candidate chain closes the exact unloaded second-strict layer to finite order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. These remain provisional pending hostile replay.`

RIGID_INTERFACE_AUDIT: `The raw-criticality reconstruction supports M_X=E_X=0 => complete A-cut, selected crossing sources in Y, x distinct physical singleton-head witnesses per source, witnesses only in tight matched endpoints or U, x<=p+u, and complementary U-code concentration. This reduces trust risk in the implication but does not solve reachability; graph regression still has zero positive fixtures.`

USED_WITNESS_INDEPENDENCE_REPLAY: `07f6ae6... independently hostile-replayed the general one-code theorem. G[W] is independent and E_W>=k_P(p+k_P-2) survives, with the orientation clarification that the valid U--U source endpoint must be chosen first. The replay explicitly did NOT prove anticompleteness from W to unused U_bar(d).`

PSI_PHYS_OPTIMIZATION: `6bb6531... exactly optimized the physical gamma/U score floor in x>=p, giving the ellipse 3R^2+4RS+4S^2<=10c^2+40c+44 and n<=6c+18+sqrt(55c^2+220c+242). This sharply compresses the large-gap region but does not bound c.`

PRIVATE_COORDINATE_EXHAUSTION: `d532bca... identifies residual Boolean dimension r=p-m=c-d_U. If k>0 then r>=1, d_U<=c-1, k>=u-c+1. For r=1 every U-certified head has the single code C=d xor e_j.`

ROOTED_Q_FEEDBACK: `4d07e27... feeds W_s independence into q=e(G[U]): q<=d_U u-d_U(d_U+1)/2 and gives the exact residual-r necessary inequality (u-c+r)(x+y-1)+phi(p-r)<=u(p-lambda)+2(c-r)u-(c-r)(c-r+1)+C0.`

R1_PREDECESSOR_PROFILE: `ccd5794... classifies r=1: K has code C=d xor e_j; matched heads h_i have support {i} or {i,j}; for p>=3 A_bar(C)=empty and |U_bar(C)|<=c-1; e(K)<=k(c-1). Generic CROWD is vacuous because k-T=1-y. aa9f246... records the exact unbounded scalar stress-test family c=p=2t,y=1,g0=2t-1,lambda=4t-2,u=x=3t,k=t+1,d_U=2t-1,m=p-1,n=10t+2, which survives the then-current aggregate score/Q/pair gates.`

R1_HEAD_WITNESS_RECYCLE: `Direct criticality of h z_h is a dead end: the original pair (s,z_h) with N(s) cap N(z_h)={h} already certifies the head-witness edge. The live object is the rooted matched hub edge z_h q_j.`

R1_HUB_THEOREM: `For r=1, q_j is adjacent to every K-head and every selected witness z_h. Raw rooted B-edge criticality forces every z_h q_j spoke into the beta orientation z_h -> q_j. Its A-witness a_h has code exactly C, hence a_h in K\{h}, h a_h is a nonedge, and N_Y(z_h)=empty. Thus N_A(z_h)={h} and k>=2.`

R1_SAME_CODE_STRENGTHENING: `The new Y-anticompleteness repairs the precise orientation gap left by the general hostile replay: for the fixed minimum-source r=1 set W_s, E(W_s,U_bar(d))=empty. If a same-code edge z z' existed, source z gives the old unique-X-head contradiction, while source z' would require a Y witness adjacent to head z, impossible because z is Y-anticomplete.`

R1_X_SPLIT: `For every U-certified h in K and every matched-covered h_i in H, the rooted edge z_h q_i cannot survive either orientation if h h_i is present. Hence E(K,H)=empty. So G[A_X]=G[K] dotcup G[H] with no cross edges.`

R1_BETA_WITNESS_GRAPH: `Each hub-spoke beta certificate supplies a_h in K\{h} with h a_h nonedge. Therefore the complement of G[K] has minimum degree at least one and e(K)<=binom(k,2)-ceil(k/2). Together with the existing complement-code capacity, 2e(K)<=min{k(k-1)-2ceil(k/2),2k(c-1)}. The map h->a_h need not be injective: P2 is per physical (source,coordinate), and here the sources z_h are distinct while all obligations use the same coordinate j; P1 likewise does not prohibit many sources at one coordinate from sharing an A-witness. Thus P1/P2 alone do not give k=O(1).`

R1_STRONGER_LX: `For p>=4, |H|=p-1>=3 and near-rigid J_X<1 gives e(H)<p/4. Combining the exact X-degree identity, Z_X>=k(x-1), the no-cross decomposition and the two e(K) bounds yields L_X > x g0-p/2+max{k(p-1)+2ceil(k/2), k(p+k-2c)}. The integer floor is recorded in ONE_CODE_R1_RESIDUAL_HUB_CRITICALITY.md. Together with E_W>=k(p+k-2), this is a non-overlapping A+U score bill.`

R1_STRESS_TEST: `On the exact aa9f246... family, the new theorem gives L_X>=8t^2-3t+2ceil((t+1)/2) and E_W>=3t^2+2t-1. The score margin remains t^2+7t-3-2ceil((t+1)/2)>0, and the strengthened rooted-Q margin remains 3t^2+8t-4-2ceil((t+1)/2)>0 for every t>=2. Therefore the family is still an unbounded parameter stress test: the new structure tightens it but scalar score/Q aggregation still does not close it.`

PROCESS_NOTE: `README remains deliberately at the independently audited public checkpoint. Same-session r=1 results stay provisional until the next adversarial replay. The branch is still conditional on reaching a rigid complete Hall cut, and zero positive actual-D2C rigid fixtures with x>=3 remain the dominant interface risk.`

NEXT_ACTION: `First hostile-replay ONE_CODE_R1_RESIDUAL_HUB_CRITICALITY.md, especially the forced beta orientation of z_h q_j, code localization a_h in K, the Y-anticompleteness step, and the K--H anticompleteness proof. If those survive, attack the beta-witness digraph h->a_h using the exact singleton equations N(z_h) cap N(a_h)={q_j}: price repeated use of one A-witness by many distinct sources, or show directed cycles force additional U/A holes. Keep exact Ccap_P/(ONE-P)/(CROWD) pair-local if invoked, but do not expect P1/P2 alone to bound k because all r=1 obligations use distinct sources at the same coordinate. In parallel keep the zero-positive actual-D2C rigid-fixture caveat active.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:

- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in the subsequent upstream repair;
- `X_3` mandatory negative control;
- zero positive actual-D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until independent adversarial replay.

## Live structural chain

`audited rooted/Hall setup`
`-> independently rederived rigid singleton-head implication`
`-> provisional unloaded second-strict finite-order closure`
`-> sharp singleton matched budget`
`-> one-code private-coordinate exhaustion r=c-d_U`
`-> hostile-replayed W independence and quadratic physical U price`
`-> exact Psi_phys ellipse + rooted-Q feedback`
`-> residual-dimension-one normal form`
`-> exact r=1 scalar escape family survives aggregate gates`
`-> residual hub q_j and forced beta spoke orientation`
`-> W_s--Y=empty and W_s--U_bar(d)=empty`
`-> K--H=empty`
`-> complement G[K] minimum degree >=1`
`-> strengthened r=1 L_X/rooted-Q bills, but the exact unbounded parameter stress test still survives scalar aggregation`.

The public README remains deliberately at the independently audited checkpoint. Do not promote these same-session rigid-interface refinements before the daily red-team replay.

## Session ledger — current invocation

**Invocation start:** 14:28:17 BST.  
**Planned preservation cutoff:** 14:55:38 BST.

Substantive units completed:

1. reread `CURRENT_STATE.md`, README, the 20 September daily red-team audit and repository commits newer than the stale handoff, reconciling the live frontier through `aa9f246...` before forward mathematics;
2. identified and preserved the direct head-witness recycling obstruction: criticality of `h z_h` is already witnessed by the original crossing certificate `(s,z_h)`, so that route cannot generate a new obligation;
3. exposed the literal residual-coordinate hub `q_j`, common to all `K` heads and their selected `W_s` witnesses, and applied raw rooted B-edge criticality to every hub spoke `z_h q_j`;
4. proved the reverse spoke orientation impossible and localized every forward beta witness to `a_h in K\{h}`, forcing `h a_h` nonedges and full `W_s--Y` anticompleteness;
5. used that Y-anticompleteness to strengthen the hostile-replayed same-code result at r=1 from `G[W_s]` independent to `E(W_s,U_bar(d))=empty`;
6. applied rooted criticality to every private-coordinate edge `z_h q_i` and proved the physical cross-layer exclusion `E(K,H)=empty`;
7. converted the hub beta certificates into a minimum-degree-one complement condition on `G[K]`, combined it with the existing complement-code capacity, and derived the sharpened `e(K)` bound;
8. combined the exact X-degree identity, near-rigid `e(H)<p/4`, `Z_X>=k(x-1)` and the new X decomposition to obtain the stronger r=1 Hamming/A-slack floor `RH-LX`;
9. fed that floor back into the rooted-Q inequality, keeping the physical escape `d_U=c-1` explicit;
10. stress-tested the exact unbounded `aa9f246...` family and proved that, despite the stronger structural bills, both score and rooted-Q margins remain positive for all `t>=2`; this is a deliberate stop/pivot result rather than another bounded scan;
11. audited P1/P2 against the new one-coordinate geometry and showed why they do not by themselves give constant k: all selected obligations have distinct physical sources at the same coordinate, so the next target must be the beta-witness digraph rather than source-coordinate uniqueness alone.

**Live unfinished line:** hostile-replay the new hub theorem, then attack the directed beta-witness relation `h -> a_h` and singleton equations `N(z_h) cap N(a_h)={q_j}`, especially repeated A-witness use and directed cycles, looking for additional located U/A holes not already absorbed by the scalar score/Q bounds.

**Stop reason:** preservation phase reached after eleven tightly connected substantive units. The exact residual-one stress family remains unbounded at the aggregate level, but its physical geometry has been narrowed substantially and the next non-scalar obstruction is explicit. No mathematical blocker and no clean-checkpoint early stop.
