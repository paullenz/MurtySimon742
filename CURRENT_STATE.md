# CURRENT_STATE.md

**Canonical repository:** `paullenz/MurtySimon742`  
**Date:** 2026-09-20  
**Active target:** eventual / sufficiently-large second-extremal structure for dense diameter-2-critical graphs around `M(n)=floor((n-1)^2/4)+1`.

`X_3` (12 vertices, 32 edges, versus `M(12)=31`) remains the mandatory negative control. The false all-order 2019 Dailly–Foucaud–Hansberg Conjecture 3 is not the target. The mixed `{4,5}` selected-excess ladder is closed. First-proof priority on Erdős #742 is not the optimization target.

<!-- CURRENT-STATUS:START -->
CHECKPOINT CLASS: `ONE_CODE_R1_K2_HALF_RAY_G1_SQRT_DEFICIT_REPLAYED_2026_09_20`

WORK MODE: `MATH`

INSPECTED PREDECESSOR: `ccc6526bbd8131ce79dfdca4351b6df5667669b1`; before forward mathematics this invocation reread CURRENT_STATE.md, README.md, the latest commit interval after `be5a0a1...`, the 20 September daily red-team audit, the H--U B-layer correction and dependency audit, the general corrected H--U capacity theorem, exact capacity-deficit decomposition, rooted layer-separation guard, corrected half-ray, residual-hub theorem and preserved H--H private-foot/U split. The audit gate remained binding. The invalid private-foot H--U chain was not reused.

LAST VERIFIED RESULT: `On the corrected half-ray p=2t,c=y=t,u=t+1,h=2t-1, the corrected H--U deficit Delta=a+b+c0 has a replayed provisional barrier Delta>=3 for t>=30. In the subbranch g=|U\P|=1, the deficit is forced to grow: at least R0>=t+1-3Delta saturated shared-source rows have m_i=0 and form an H-clique; their distinct reverse-U witnesses and W_s are H-anticomplete, leaving at most 3Delta-3 residual-bar carriers for their unique P-neighbour slots. The replayed private-spoke theorem bounds each carrier to R(t)=floor((1+sqrt(8t+9))/2) such clique rows because every touched pair loses both private-foot H--H orientations and must be U-certified. Hence t+1-3Delta <= (3Delta-3)R(t), so Delta >= ceil((t+1+3R(t))/(3(R(t)+1))) = (1/(3sqrt(2))+o(1))sqrt(t). An independent same-session hostile replay checked the source-row count, exact P-reservoir count, clique property, carrier coverage, absence of cross-carrier U-cert double counting, algebra and asymptotics; no gap was found. This is the first replayed scaling lower bound on the corrected H--U capacity deficit along the intermediate half-ray, but only for g=1.`

UNPRESERVED WORK: `None after preservation. New notes this invocation: ONE_CODE_R1_K2_HALF_RAY_HU_DEFICIT_TWO_BARRIER.md; ONE_CODE_R1_K2_HALF_RAY_SMALL_DEFICIT_CONCENTRATION.md; ONE_CODE_R1_K2_RESIDUAL_BAR_HUB_PRIVATE_SPOKE_OBSTRUCTION.md; ONE_CODE_R1_K2_HALF_RAY_DELTA_THREE_BARRIER.md; ONE_CODE_R1_K2_HALF_RAY_DELTA_THREE_HOSTILE_REPLAY.md; ONE_CODE_R1_K2_HALF_RAY_G1_SQRT_DEFICIT.md; ONE_CODE_R1_K2_HALF_RAY_G1_SQRT_DEFICIT_HOSTILE_REPLAY.md. The invalid H--U private-foot coordinate-slice chain remains superseded.`

DEFERRED ADMIN: `README remains deliberately at the independently audited public checkpoint. Do not promote these same-session results before the next daily hostile audit. Historical invalid claims remain preserved as superseded evidence.`

NEXT ACTION: `Generalize the replayed g=1 argument to 1<g<=Delta. A saturated shared-source row obeys m_i<=a_i+g-s_i, so most source rows have missing H-degree O(g), not zero. Prove a bounded-missing-degree carrier theorem: if a residual-bar carrier touches r H-rows each with missing degree <=q, then all but O(rq) pairs among them are H-edges and lose both private-foot orientations, forcing binom(r,2)-O(rq)<=u. Combine that with T>=t-O(Delta), <=Delta outside heads and the small number of non-witness P-carriers to seek Delta=Omega(sqrt(t)) for arbitrary g, then bootstrap through H-hole/score feedback. Keep the general corrected H-slack term (kappa-theta)_+ off-ray and preserve physical currencies. Separately retain the audit-priority rigid-interface coverage gap; do not infer realizability from this conditional branch.`

AUDIT GATE: `The 20 September daily red-team audit remains binding. P1/P2 semantics are repaired; raw same-code criticality and ordered (source,witness) injection independently passed later repair; X_3 remains mandatory; exact pair-local Ccap_P/(ONE-P)/(CROWD) remain load-bearing whenever invoked; finite scans are diagnostics only; superseded evidence stays superseded; bounded actual-D2C regression still has zero positive rigid complete Hall-cut fixtures with x>=3. Rooted layer separation is mandatory lint: a B-source cannot use a B-witness for a non-root singleton head.`

SECOND STRICT: `Provisional exact unloaded second-strict chain remains finite-order: mixed y=1 x=3 gives n<=16; x=4 n<=19; x>=5 n<=33; two-X-hole arm n<=816; (0,2) empty; mixed y>=2 internally closed. Conditional on the rigid one-code interface and predecessor closures, no exact unloaded second-strict survivor remains for n>=817. Pending hostile replay.`

R1 K2 LIVE CHAIN: `Residual-one common hub/beta fan -> k=2,J2=empty -> global H/Y polarization -> B0 source collapse -> e(Y)=0 -> corrected H--U B-layer witness classification -> capacity-deficit decomposition -> residual-column localization -> small-Delta concentration -> raw private-spoke carrier restriction -> replayed provisional Delta>=3 for t>=30 -> replayed g=1 Delta=Omega(sqrt(t)) -> next: bounded-missing-degree carrier theorem for general g.`

R1 K2 SUPERSESSION: `Do not return to the exact kappa=theta=1 ray, golden dual-witness model, literal b=0 half-ray, or invalid HU-PRIVATE/HU-QI/HU-IHOLE H--U chain. The current half-ray frontier is corrected B-layer capacity plus residual-column concentration and raw private-spoke/H--H carrier criticality.`
<!-- CURRENT-STATUS:END -->

---

## Binding audit gate

Latest daily audit: `project/research/post_ms/2026-09-20-daily-red-team-audit-v1/DAILY_RED_TEAM_AUDIT.md`.

Binding constraints:
- repaired P1/P2 semantics;
- raw same-code criticality / ordered `(source,witness)` injection independently passed in later repair;
- `X_3` mandatory negative control;
- zero positive actual D2C rigid complete Hall cuts with `x>=3` in bounded regression;
- exact pair-local `Ccap_P`, `(ONE-P)`, `(CROWD)` whenever invoked;
- finite scans are diagnostics only;
- superseded evidence stays superseded;
- same-session closures stay provisional until hostile replay;
- rooted layer separation: if source and witness both lie in `B=N(v)`, they share the root and cannot define a non-root singleton common-neighbour equation.

## Session ledger — current invocation

**Invocation start:** 22:26:59 BST.  
**Planned preservation cutoff:** 22:55:38 BST.

Substantive units completed:
1. reconciled handoff, README, latest commits and daily red-team audit;
2. accepted the B-layer correction and abandoned the invalid private-foot H--U chain;
3. proved shared resources lie in the residual-bar U column and `g<=c0`;
4. derived the exact residual-slot H--U degree/deficit inequality;
5. derived the quantitative Delta lower bound from H-holes;
6. derived the independent shared-edge placement inequality;
7. completely excluded Delta=1 for t>=5;
8. derived local row deficit accounting and `T>=t-1-Delta-2b`;
9. forced saturated-row shared load into reverse-U edges through <=Delta residual-plus heads with distinct witnesses;
10. bounded collective H-degree of those witnesses by `2Delta M_H`;
11. constructed a capacity-saturating abstract Delta=2 hostile normal form;
12. replayed it against the preserved H--H split and found capacity alone does not kill it;
13. proved the raw private-spoke obstruction for an H-universal residual-bar U vertex;
14. used H--H certificate capacity to kill the specific Delta=2 hostile normal form;
15. excluded the three c0=0 Delta=2 allocations by placement/local row capacity;
16. excluded `(1,0,1)` by P-exhaustion witness incidence plus the exact residual-slot degree bound;
17. generalized private-spoke criticality to classify an H-positive P-carrier's private d-support as empty or exactly two coordinates;
18. converted that classification into a per-carrier clique-touch bound `binom(r,2)<=u`;
19. applied the carrier bound to `(0,1,1)` and excluded it for t>=30;
20. concluded `Delta>=3`, equivalently `L_H>=3t+2`, for t>=30;
21. independently hostile-replayed the complete Delta-three proof; replay passed;
22. specialized to g=1 and proved at least `R0>=t+1-3Delta` shared-source rows have `m_i=0`;
23. showed their H-anticomplete reverse witnesses leave at most `3Delta-3` residual-bar carriers;
24. combined the carrier H--H cap with that cover to obtain `Delta=Omega(sqrt(t))` explicitly;
25. independently hostile-replayed the g=1 square-root theorem, including resource counts and non-double-counting; replay passed at stated conditional scope.

**Live unfinished line:** bounded-missing-degree carrier theorem for arbitrary `1<g<=Delta`, then global square-root/bootstrapped deficit growth if possible.

**Stop reason:** preservation phase reached; the corrected/replayed Delta-three theorem and replayed g=1 scaling advance were durably preserved before the next research invocation.
